# AcOrP Attention

Updated: 2026-04-26

## Hard Blockers

- GitHub push/auth is blocked: local commit `c490dca` could not be pushed because GitHub credentials are invalid. Do not spend engineering time on auth until credentials are refreshed.

## Recent Review Decisions

- Strategic AI site-control proof reporting is recorded in `docs/strategic-ai-capture-countercapture-defense-proof-report.md` with focused Godot coverage in `tests/ai_site_control_proof_report.gd`. The gate passes for the selected Mireclaw `river-pass` signal-yard path: `river_free_company` and `river_signal_post` rank first and second when player controlled, assignment and seizure events stay compact, `river_free_company` flips from `player` to `faction_mireclaw` under current raid resolution, and the full selector can still legitimately choose `riverwatch_hold` as the town-front sanity result.
- No site-control coefficient defect was found. Continue to defer coefficient tuning unless a later report or manual pass shows a specific ordering, defense, seizure, or retake defect.
- Strategic AI Glassroad defense proof planning is recorded in `docs/strategic-ai-glassroad-defense-proof-plan.md`. The selected fixture is `glassroad-sundering`, the direct Embercourt enemy scenario; the proof centers on `glassroad_watch_relay` as the primary relay site, `glassroad_starlens` as the companion stabilization/reason surface, `halo_spire_bridgehead` as the town-front sanity check, `glassroad_beacon_wardens` as objective/encounter context, and `riverwatch_market` town-governor garrison stabilization as the defense signal.
- Strategic AI Glassroad defense proof reporting is recorded in `docs/strategic-ai-glassroad-defense-proof-report.md` with focused Godot coverage in `tests/ai_glassroad_defense_proof_report.gd`. The gate passes for the selected Embercourt `glassroad-sundering` path: `glassroad_watch_relay` and `glassroad_starlens` rank first and second when player controlled, `halo_spire_bridgehead` remains the accepted full-selector town-front sanity target, relay assignment and staged relay retake/controller flip work, Starlens exposes its stabilization profile, Riverwatch town-governor garrison stabilization is present, and public event records do not leak score-table fields.
- No Glassroad coefficient defect, relay retake blocker, or defense-specific durable state need was found. Continue to defer coefficient tuning and defense-specific durable state unless a later report or manual pass shows a specific ordering, defense, seizure, retake, or public-surface defect.
- Strategic AI commander-role state boundary planning is recorded in `docs/strategic-ai-commander-role-state-plan.md`. The plan keeps current raid encounter fields and commander roster continuity as compatibility state, defines a future minimal `commander_role_state` boundary for non-staged assignment, defense, raid, recovery, retake, stabilizer, and personality-pressure reporting, and keeps schema implementation/save migration/full AI hero task state deferred.
- Strategic AI commander-role report fixture planning is recorded in `docs/strategic-ai-commander-role-report-fixture-plan.md`. The plan defines future `AI_COMMANDER_ROLE_STATE_REPORT` cases and payload expectations for Mireclaw Free Company retaker/raider, Mireclaw Signal Post companion, Embercourt Glassroad relay defender/retaker/stabilizer, commander recovery blocking assignment, commander memory continuity, public leak checks, pass/fail criteria, validation commands, and staged next steps.
- Strategic AI minimal commander-role state schema planning is recorded in `docs/strategic-ai-minimal-commander-role-state-schema-plan.md`. The plan selects `session.overworld.enemy_states[].commander_roster[].commander_role_state` as the future optional field location; defines the minimal fields, enum sets, defaults, old-save compatibility, deterministic assignment id derivation, derived front id rules, public-reason recomputation, fixture-only annotation boundary, validation strategy, migration risks, and adapter path to later full AI hero state.
- Strategic AI commander-role report implementation boundary planning is recorded in `docs/strategic-ai-commander-role-report-implementation-plan.md`. The plan assigns future helper ownership to `EnemyAdventureRules.gd`, keeps `EnemyTurnRules.gd` as supporting town-governor evidence only, defines report-only fixture normalization, future report scene shape, public leak checks, validation commands, and exact implementation sequence.
- Strategic AI commander-role report implementation is recorded in `docs/strategic-ai-commander-role-report-implementation-report.md` with focused Godot coverage in `tests/ai_commander_role_state_report.gd`. The report covers all eight planned `AI_COMMANDER_ROLE_STATE_REPORT` cases: Mireclaw Free Company retaker/raider, Signal Post companion, Embercourt Glassroad relay defender/retaker/stabilizer, recovery blocking assignment, and memory continuity. Public role events stay compact and leak checks pass.
- Strategic AI commander-role report gate review is recorded in `docs/strategic-ai-commander-role-report-gate-review.md`. The gate passes: the focused report rerun printed `AI_COMMANDER_ROLE_STATE_REPORT` with `"ok": true`, `schema_status: "report_fixture_only"`, all eight planned cases, and a passing public leak check across eight compact role events.
- Strategic AI commander-role adoption sequencing planning is recorded in `docs/strategic-ai-commander-role-adoption-sequencing-plan.md`. The plan compares derived live-turn transcript/report surfacing, minimal `commander_role_state` write/read adoption, full AI hero task-state sequencing, and pausing commander roles; it recommends behavior-neutral derived live-turn transcript/report planning next.
- Strategic AI commander-role live-turn transcript/report planning is recorded in `docs/strategic-ai-commander-role-live-turn-transcript-report-plan.md`. The plan defines future `AI_COMMANDER_ROLE_TURN_TRANSCRIPT_REPORT` ownership, payload shape, River Pass and Glassroad fixtures, enemy-turn phase records, active commander links, before/after derived role proposals, target assignment/no-op reasons, raid movement/arrival summaries, town-governor supporting event references, compact public transcript events, public leak checks, validation commands, failure conditions, and exact implementation sequence.
- Strategic AI commander-role live-turn transcript/report implementation is recorded in `docs/strategic-ai-commander-role-live-turn-transcript-report-implementation-report.md` with focused Godot coverage in `tests/ai_commander_role_turn_transcript_report.gd`. The report calls existing `EnemyTurnRules.run_enemy_turn(...)` once for deterministic River Pass and Glassroad fixture sessions, then derives phase records, commander links, role proposals, assignment/no-op records, raid movement/arrival summaries, town-governor refs, compact public transcript events, and leak/source-marker checks without changing live behavior.
- Strategic AI commander-role live-turn transcript/report gate review is recorded in `docs/strategic-ai-commander-role-live-turn-transcript-report-gate-review.md`. The gate passes: the focused report rerun printed `AI_COMMANDER_ROLE_TURN_TRANSCRIPT_REPORT` with `ok: true`, `schema_status: "derived_turn_transcript_report_only"`, `behavior_policy: "observe_existing_enemy_turn_only"`, `save_policy: "no_commander_role_state_write"`, River Pass Free Company retake/controller flip, Glassroad Watch Relay retake/controller flip, source-marker checks, and public leak checks across 21 compact public transcript events.
- The next current slice is `strategic-ai-hero-task-state-boundary-planning-10184`: planning-only strategic AI hero task-state boundary work before any schema writes, save migration, durable event logs, defense-specific durable state, coefficient tuning, full AI hero task-state implementation, production JSON edits, or live commander-role behavior.
- Defense-specific durable state is not approved. Keep using current compact event vocabulary (`ai_target_assigned`, `ai_pressure_summary`, `ai_site_seized`, `ai_site_contested`, and town-governor events) because the Glassroad report proved current assignment/seizure/town-governor surfaces can express the selected defense behavior.
- Commander-role report gate review, adoption sequencing, live-turn transcript/report planning, live-turn transcript/report implementation, and live-turn transcript/report gate review are complete, after both Mireclaw site-control and Embercourt Glassroad defense reports passed and the commander-role boundary, report fixture, minimal schema, implementation-boundary plans, and report implementation completed without tuning or defense-state blockers.
- Overworld object additive report review is recorded in `docs/overworld-object-report-review-001.md`. Current report output is acceptable as compatibility-warning noise: 43 map objects, 48 resource sites, 127 site placements, 48 encounter placements, 169 warnings, and 0 errors.
- Current production object/resource-site JSON should remain warning-only for missing future metadata. Strict object-schema errors stay limited to `tests/fixtures/overworld_object_schema/` until a later production bundle explicitly declares the migrated schema.
- First production additive schema planning for safe metadata fields is recorded in `docs/overworld-object-safe-additive-schema-plan.md`. The approved-for-planning safe field set is `schema_version`, `primary_class`, `secondary_tags`, footprint anchor/tier, `passability_class`, and `interaction`.
- The first small production migrated bundle is implemented as `safe_metadata_bundle_001`: `object_waystone_cache`, `object_timber_wagon`, `object_watchtower_beacon`, `object_wayfarer_infirmary`, `object_market_caravanserai`, `object_brightwood_sawmill`, `object_bramble_wall`, and `object_ember_signal_brazier`. The migration is runtime-inactive safe metadata only.
- Migrated-bundle validation is strict only for the safe fields on those eight ids. All other production map objects remain compatibility-warning-only until a later bundle is explicitly declared.
- `body_tiles`, `approach`, route effects, animation cue ids, editor placement hints, AI hints, placed-site object links, and first-class neutral encounter records should be planned as staged follow-ups. They should not switch pathing, renderer, AI, editor, or runtime behavior yet.
- First-class visible neutral encounter records are accepted as a needed schema direction, and the representation plus additive validator/report planning decisions are now documented. Production object JSON migration still requires report/fixture implementation and review first.
- Neutral encounter representation planning is recorded in `docs/neutral-encounter-representation-plan.md`. The target decision is a hybrid model: `visible_stack` is the default representation, `camp_anchor` is an explicit fixed-site subtype, and guard-linked variants carry guard-target links when an encounter protects another placement. Current direct scenario encounter placements remain the compatibility source; no production JSON migration or runtime behavior change is approved.
- Current scenario encounter reality is 48 direct `scenarios[].encounters[]` placements with `placement_id`, `encounter_id`, coordinates, difficulty, combat seed, and occasional field objectives. There are no first-class `neutral_encounter` map-object records, object ids, or guard-target links in production content today.
- The first tiny neutral encounter candidate bundle is planning-only: `river_pass_ghoul_grove`, `river_pass_hollow_mire`, and `ninefold_basalt_gatehouse_watch`. Do not implement it as production metadata until the opt-in report/fixture scaffolding lands and its output is reviewed.
- Neutral encounter additive validator/report planning is recorded in `docs/neutral-encounter-additive-validator-report-plan.md`. It recommends a separate opt-in `--neutral-encounter-report` / JSON report / strict fixture CLI path rather than expanding `--overworld-object-report`, because the neutral report is scenario-placement-centric.
- The neutral encounter report contract keeps current production direct placements compatibility-warning-only. Strictness is limited to future non-production fixtures for `visible_stack`, `camp_anchor`, `guard_linked_stack`, `guard_linked_camp`, and invalid missing cue/link cases.
- Neutral encounter additive validator/report scaffolding is complete locally as opt-in `tests/validate_repo.py` support plus non-production fixtures under `tests/fixtures/neutral_encounter_schema/`. It adds `--neutral-encounter-report`, `--neutral-encounter-report-json`, and `--strict-neutral-encounter-fixtures`.
- The opt-in neutral encounter report currently shows 48 direct placements across 15 scenarios, 62 encounter definitions, 36 script-spawn advisory effects, 0 first-class neutral encounter objects, 4 placement-level field-objective overrides, 23 definition-backed field-objective placements, and expected missing future metadata warning counts of 48 for representation, danger, guard, state, ownership, reward, passability, AI, and editor metadata.
- Neutral encounter additive report review is recorded in `docs/neutral-encounter-report-review-001.md`. Current report output is acceptable compatibility-warning noise: 15 scenarios, 62 encounter definitions, 48 direct placements, 36 script-spawn advisory effects, 10 repeated encounter ids, 4 placement-level field-objective overrides, 23 definition-backed field-objective placements, 435 warnings, and 0 errors.
- Current production direct encounter placements remain compatibility-warning-only. Strict neutral encounter errors stay limited to `tests/fixtures/neutral_encounter_schema/` until a later production bundle explicitly declares the migrated schema.
- Planning-only metadata for `neutral_encounter_representation_bundle_001` is recorded in `docs/neutral-encounter-representation-bundle-001-plan.md`. It recommends additive scenario encounter placement extensions under `neutral_encounter` for the first bundle, not a sidecar file and not first-class map-object records yet.
- The planned bundle is limited to `river_pass_ghoul_grove`, `river_pass_hollow_mire`, and `ninefold_basalt_gatehouse_watch`. It preserves `placement_id`, `encounter_id`, `difficulty`, `combat_seed`, and field-objective behavior.
- The basalt gatehouse guard link should use `target_kind: "resource_node"`, `target_id: "site_basalt_gatehouse"`, and `target_placement_id: "dwelling_basalt_gatehouse"`. The site id is the content target; the dwelling placement id is the scenario resource-node target.
- Metadata-only implementation for `neutral_encounter_representation_bundle_001` is complete locally. `content/scenarios.json` now adds `neutral_encounter` placement metadata only to `river_pass_ghoul_grove`, `river_pass_hollow_mire`, and `ninefold_basalt_gatehouse_watch`; `tests/validate_repo.py` validates that migrated bundle and reports authored representation/guard-link modes distinctly.
- The authored neutral encounter report now marks `neutral_encounter_representation_bundle_001` as `metadata_authored`, reduces authored missing future metadata counts from 48 to 45, and keeps the remaining 45 direct placements compatibility-warning-only.
- Authored bundle report review is recorded in `docs/neutral-encounter-representation-bundle-001-report-review.md`. The report shows `production_json_migration_scope: "scenario_placement_metadata_only"`, 3 authored placements, 45 remaining inferred placements, authored representation counts of 2 `visible_stack` and 1 `guard_linked_stack`, authored guard roles `none`, `route_block`, and `guards_resource_node`, 408 warnings, and 0 errors.
- The basalt gatehouse guard-link target convention is confirmed from source and report: `target_kind: "resource_node"`, `target_id: "site_basalt_gatehouse"`, and `target_placement_id: "dwelling_basalt_gatehouse"`.
- Remaining production direct encounter placements stay compatibility-warning-only; strict neutral encounter schema coverage stays fixture-only; migrated-bundle strict validation now applies only to the declared three authored placements.
- The next allowed neutral encounter slice is first-class neutral encounter object planning before any broader metadata bundle, runtime encounter behavior, pathing, AI, editor behavior, renderer behavior, save format, generated PNG import, `body_tiles`, or approach adoption.
- First-class neutral encounter object migration-boundary planning is recorded in `docs/neutral-encounter-first-class-object-migration-plan.md`. The plan keeps direct scenario encounter placements as the current authority until object-backed records buy a real boundary: editor/object placement, object schema fields, guard-target validation, renderer/pathing/AI/save readiness, or reusable object definitions.
- Future first-class neutral encounter records should preserve links back to existing `placement_id`, `encounter_id`, scenario coordinates, field-objective source, guard targets, resource-site/object/town targets, and authored metadata bundle ids. The existing three `neutral_encounter_representation_bundle_001` records remain valid scenario-placement metadata and may later be lifted into object-backed records with exact agreement.
- Opt-in first-class neutral encounter object report and strict fixture scaffolding is complete locally. The neutral encounter report now distinguishes direct-only, scenario-metadata, object-backed, and lifted records; production currently reports 45 direct-only placements, 3 scenario-metadata placements, 0 object-backed placements, and 0 lifted records.
- The updated neutral encounter report tracks missing `object_id`, missing `object_placement_id`, missing lifted metadata agreement, missing guard target resolution, and missing object schema fields as report-only warnings. Current production output has 48 missing object ids, 48 missing object-placement ids, 0 missing lifted agreements, 0 missing guard-target resolutions, 48 missing object-schema-field records, 553 warnings, and 0 errors.
- Strict neutral encounter fixtures now include object-backed `visible_stack`, `camp_anchor`, `guard_linked_stack`, and `guard_linked_camp` records, plus invalid cases for missing `object_id`, missing legacy `placement_id` bridge, missing encounter ref, mismatched field-objective source, mismatched guard target ids, and duplicate scenario/object authority disagreement.
- First-class object report review is recorded in `docs/neutral-encounter-first-class-object-report-review.md`. Current production output has 45 direct-only placements, 3 scenario-metadata placements, 0 object-backed placements, 0 lifted records, 48 missing object ids, 48 missing object-placement ids, 48 missing object-schema-field records, 553 warnings, and 0 errors.
- The report review confirms guard-target resolution has 0 missing required resolutions in the current report state; basalt gatehouse remains the advisory scenario-metadata example using `target_id: "site_basalt_gatehouse"` plus `target_placement_id: "dwelling_basalt_gatehouse"`.
- Strict object-backed neutral encounter fixture coverage now includes valid `visible_stack`, `camp_anchor`, `guard_linked_stack`, and `guard_linked_camp` records, plus six invalid object-backed authority/link cases. This remains fixture-only and does not authorize production object-backed records.
- Current production direct-only and scenario-metadata placements remain compatibility-warning-only for first-class object fields. Migrated-bundle strict production checks remain limited to the existing scenario-metadata `neutral_encounter_representation_bundle_001`.
- The next allowed neutral encounter slice is planning-only `neutral_encounter_first_class_object_bundle_001`, preferably lifting the existing three authored `neutral_encounter_representation_bundle_001` placements into proposed object-backed records. It should not edit production JSON or change runtime encounter behavior, pathing, AI, editor behavior, renderer behavior, save migration, generated PNG import, or asset import.
- Planning-only `neutral_encounter_first_class_object_bundle_001` is recorded in `docs/neutral-encounter-first-class-object-bundle-001-plan.md`. It lifts exactly `river_pass_ghoul_grove`, `river_pass_hollow_mire`, and `ninefold_basalt_gatehouse_watch` from `neutral_encounter_representation_bundle_001` into proposed object-backed records without editing production JSON.
- Proposed object ids are `object_neutral_encounter_river_pass_ghoul_grove_stack`, `object_neutral_encounter_river_pass_hollow_mire_stack`, and `object_neutral_encounter_ninefold_basalt_gatehouse_watch_stack`; proposed object placement ids are `object_placement_river_pass_ghoul_grove`, `object_placement_river_pass_hollow_mire`, and `object_placement_ninefold_basalt_gatehouse_watch`.
- The bundle plan preserves legacy placement bridges, encounter refs, exact lifted metadata agreement, object-schema fields, field-objective source, basalt guard-target convention, rollback, and non-change boundaries. Expected later report delta is 3 object-backed/lifted records and missing object id/object-placement id/object-schema-field records reduced from 48 to 45.
- Metadata-only production object-backed implementation for `neutral_encounter_first_class_object_bundle_001` is complete locally for exactly those three records. `content/map_objects.json` now has three `primary_class: "neutral_encounter"` object records, and the three existing direct scenario encounter placements now carry object-backed bridge metadata while preserving their original `placement_id`, `encounter_id`, `x`, `y`, `difficulty`, `combat_seed`, and field-objective behavior.
- The opt-in neutral encounter report now shows 3 first-class neutral encounter objects, 3 object-backed placements, 3 lifted records, missing `object_id` / `object_placement_id` / object-schema-field records reduced from 48 to 45, missing lifted agreement 0, and missing guard-target resolution 0. Runtime, pathing, renderer, AI, editor, and save adoption remain inactive.
- Strict production checks now cover only `neutral_encounter_representation_bundle_001` and `neutral_encounter_first_class_object_bundle_001`; the remaining 45 direct encounter placements stay compatibility-warning-only.
- The next allowed neutral encounter slice is report review/follow-up for the implemented object-backed bundle. Runtime encounter behavior, pathing, AI, editor behavior, renderer behavior, save migration, generated PNG import, asset import, `body_tiles`, approach adoption, route effects, and broader production migration remain out of scope.
- Implemented object-backed bundle report review is recorded in `docs/neutral-encounter-first-class-object-bundle-001-report-review.md`. The reviewed report shows 3 first-class neutral encounter objects, 3 object-backed placements, 3 lifted records, 45 remaining missing object ids/object-placement ids/object-schema-field records, 0 missing lifted agreements, 0 missing guard-target resolutions, 542 warnings, and 0 errors.
- The three object records and three scenario bridge records preserve the planned ids, legacy placement fields, encounter refs, lifted metadata, and bundle provenance. Basalt gatehouse guard-target resolution is confirmed: `target_kind: "resource_node"`, `target_id: "site_basalt_gatehouse"`, and `target_placement_id: "dwelling_basalt_gatehouse"`.
- The remaining 45 direct encounter placements stay compatibility-warning-only. Strict production checks cover only `neutral_encounter_representation_bundle_001` and `neutral_encounter_first_class_object_bundle_001`; broader object-backed neutral migration, runtime/pathing/renderer/AI/editor/save adoption, `body_tiles`, approach, route effects, and generated asset import remain out of scope.
- The prior recommendation to pause neutral encounter metadata migration and run `foundation-game-loop-prioritization-10184` is complete. Another tiny neutral bundle would mostly reduce warning count without improving player-facing or tooling behavior.
- Foundation/game-loop prioritization is recorded in `docs/foundation-game-loop-prioritization.md`. It compares economy capture/resource loop, object/pathing/editor adoption, strategic AI turn pressure, live-client proof, animation/screen feedback, and concept-art-to-asset pipeline.
- The selected next track is economy capture/resource loop proof, starting with planning slice `economy-capture-resource-loop-proof-planning-10184`. The rationale is that capture/resource/town-spend pressure has the best player-facing leverage, uses existing resource-site/town/report foundations, and can be validated without immediate rare-resource migration, pathing adoption, renderer work, AI rewrite, editor adoption, save migration, or asset import.
- Economy capture/resource loop proof planning is recorded in `docs/economy-capture-resource-loop-proof-plan.md`. The selected target is the Riverwatch signal-yard path in `river-pass`: `north_timber`, `river_signal_post`, `river_free_company`, `river_pass_ghoul_grove`, `river_pass_hollow_mire`, `southern_ore`, optional `eastern_cache`, and Riverwatch build/recruit choices using current `gold`, `wood`, and `ore`.
- Economy capture/resource loop live proof is recorded in `docs/economy-capture-resource-loop-live-proof-report.md`. Current authored data and current Godot core systems prove the selected Riverwatch signal-yard path without production code/content changes: `north_timber`, `river_signal_post`, `river_free_company`, `river_pass_ghoul_grove`, `river_pass_hollow_mire`, `southern_ore`, optional `eastern_cache`, Riverwatch `building_bowyer_lodge`, `unit_ember_archer` recruitment, and save/resume all preserve expected economy state.
- Economy capture/resource loop manual gate review is recorded in `docs/economy-capture-resource-loop-manual-gate-review.md`. The gate passes for the current-systems proof. The reviewed evidence covers current `gold`/`wood`/`ore` resources, persistent daily income from `river_signal_post` and `river_free_company`, immediate recruit joins, Ghoul Grove and Hollow Mire route/resource effects, `southern_ore`, optional `eastern_cache`, Riverwatch `building_bowyer_lodge`, `unit_ember_archer` recruitment, save/resume preservation, and plausible scenario continuation.
- An exact routed live-client transcript for the selected economy route is deferred. Build it later only if AcOrP explicitly requests repeatable transcript coverage or a regression makes it necessary; do not add broad harness work now.
- Strategic AI economy pressure planning is recorded in `docs/strategic-ai-economy-pressure-slice-plan.md`. The first target is intentionally narrow: keep the current River Pass / Duskfen pressure model and make it explainably value and contest player-controlled `river_free_company` and `river_signal_post`, with common-resource pickups, fight-gated routes, Riverwatch spend/recruit decisions, save/resume continuity, and remaining objectives as the evidence base.
- Strategic AI economy pressure implementation is recorded in `docs/strategic-ai-economy-pressure-implementation-report.md`. `EnemyAdventureRules` now exposes resource target score breakdown/report helpers and uses the breakdown for resource candidate priority. The focused Godot report proves player-controlled `river_signal_post` and `river_free_company` outrank simple pickups in River Pass resource ordering while the full target selector can still prefer `riverwatch_hold` when town pressure legitimately dominates.
- Strategic AI economy pressure report gate review is recorded in `docs/strategic-ai-economy-pressure-report-gate-review.md`. The gate passes: player-owned `river_signal_post` outranks simple pickups in the focused reachable report; player-owned `river_free_company` and `river_signal_post` rank first and second when both are owned; target reasons are explainable; no hidden difficulty bonus or broad rewrite was found; and the full selector can still prefer `riverwatch_hold` when town pressure legitimately dominates.
- Strategic AI event/threat reason surfacing planning is recorded in `docs/strategic-ai-event-surfacing-plan.md`. The plan defines a compact two-surface model: minimal public AI event/threat reasons for target assignment, site contest, site seizure, and pressure summaries, with full score breakdowns kept debug/report-only.
- The first examples for event surfacing are the River Pass signal-yard pressure surfaces: `river_free_company` assignment/seizure reasons, `river_signal_post` assignment/seizure reasons, `riverwatch_hold` town-pressure dominance, and reusable site contest/seizure records.
- Strategic AI event/threat reason surfacing implementation is recorded in `docs/strategic-ai-event-surfacing-implementation-report.md`. `EnemyAdventureRules` now builds reusable ephemeral/derived `ai_target_assigned`, `ai_pressure_summary`, `ai_site_seized`, and `ai_site_contested` records, and threat/dispatch surfaces can show one compact public reason phrase without exposing score tables.
- The focused Godot report `tests/ai_event_surfacing_report.tscn` proves River Pass `river_free_company`, `river_signal_post`, `riverwatch_hold`, resource-site seizure, and `river_pass_ghoul_grove` objective-contest examples. It also checks public events and threat/dispatch text do not leak full score table fields.
- Strategic AI event/threat reason surfacing report gate review is recorded in `docs/strategic-ai-event-surfacing-report-gate-review.md`. The gate passes: the focused event report and economy pressure report both pass, compact public reason output is present for assignment/pressure/seizure/contest/threat/dispatch surfaces, and no public event or threat/dispatch text leaks score-table fields such as `final_priority`, `base_value`, or `assignment_penalty`.
- A manual live-client signal-yard enemy-turn gate is deferred for now. Run it later only if AcOrP asks for it or a later AI expansion affects visible turn pacing, arrival frequency, map pressure, or UI composition.
- Strategic AI pressure expansion planning is recorded in `docs/strategic-ai-pressure-expansion-plan.md`. The selected next boundary was behavior-neutral town governor pressure reporting rather than durable event streams, full AI hero roster/task state, faction-wide personality tuning, another resource-only proof, or concept-art deferral.
- Strategic AI town governor pressure report implementation is recorded in `docs/strategic-ai-town-governor-pressure-implementation-report.md`. `EnemyTurnRules` now exposes behavior-neutral town governor reports, build score breakdowns, recruitment destination breakdowns, and compact derived events for `ai_town_built`, `ai_town_recruited`, `ai_garrison_reinforced`, `ai_raid_reinforced`, and `ai_commander_rebuilt`.
- The focused Godot report `tests/ai_town_governor_pressure_report.tscn` prints `AI_TOWN_GOVERNOR_PRESSURE_REPORT` for River Pass Duskfen / Mireclaw, covering selected build reasoning, garrison reinforcement, active raid reinforcement, commander rebuild, and public-event leak checks.
- Strategic AI town governor pressure report gate review is recorded in `docs/strategic-ai-town-governor-pressure-report-gate-review.md`. The gate passes: Duskfen selects Slingers Post, recruitment routes to garrison stabilization, active raid reinforcement, and commander rebuild in the expected focused cases, compact derived events do not leak score-table fields, and the report-only boundary holds.
- A live-client enemy-turn gate is deferred for now. Run it later only if AcOrP asks for it or a later AI slice connects town-governor events to normal player-facing turn text, changes visible turn pacing, arrival frequency, map pressure, or UI composition.
- Strategic AI faction personality pressure planning is recorded in `docs/strategic-ai-faction-personality-pressure-plan.md`. It compares Embercourt and Mireclaw as the first personality anchors and defines usable evidence surfaces before tuning: target preferences, town build reasons, recruitment destination reasons, garrison/raid/commander priorities, and compact public reason phrases.
- Strategic AI faction personality evidence reporting is recorded in `docs/strategic-ai-faction-personality-evidence-report.md`. The focused Godot report `tests/ai_faction_personality_evidence_report.tscn` prints `AI_FACTION_PERSONALITY_EVIDENCE_REPORT` comparing Embercourt and Mireclaw under one target/event/town-governor vocabulary.
- The evidence report supports Embercourt as town-front/civic infrastructure pressure and Mireclaw as raid/resource-denial pressure, but keeps weak, contradicted, and missing claims separate. Important contradiction: `prismhearth-watch` is a Mireclaw enemy fixture, not an Embercourt one; `glassroad-sundering` is the current direct Embercourt enemy fixture.
- Strategic AI strategy config audit planning is recorded in `docs/strategic-ai-strategy-config-audit-plan.md`. It defines the narrow audit categories before tuning: faction base strategy weights, scenario overrides, public reason phrase vocabulary, resource/site family priorities, build category/value weights, reinforcement/garrison/raid/commander rebuild bias, validation/report commands, and manual gate triggers.
- Strategic AI strategy config audit reporting is recorded in `docs/strategic-ai-strategy-config-audit-report.md`. It classifies most current Embercourt and Mireclaw base weights and River Pass/Prismhearth/Glassroad overrides as supported, identifies questionable Embercourt public build wording, possible later infrastructure weighting review, and Ninefold Mireclaw broad-map sharpness, and preserves the fixture reality that `prismhearth-watch` is Mireclaw while `glassroad-sundering` is direct Embercourt.
- Strategic AI site-control proof reporting is recorded in `docs/strategic-ai-capture-countercapture-defense-proof-report.md`. The focused report proved Mireclaw retaking/denying `river_free_company`, confirmed `river_signal_post` companion denial reasons, preserved `riverwatch_hold` town-front selector sanity, and found no coefficient defect. The Glassroad defense proof plan is now complete in `docs/strategic-ai-glassroad-defense-proof-plan.md`.
- Continue to avoid production JSON migration, `content/resources.json`, `wood` to `timber` migration, rare-resource activation, market-cap overhaul, pathing/body-tile/approach adoption, renderer/editor/save behavior changes, generated PNG import, neutral encounter bundle migration, durable AI event log migration, full AI hero task state, broad strategic AI rewrite, behavior tuning, or broad River Pass rebalance.
- Neutral encounter metadata migration remains paused. The remaining 45 direct encounter placements stay compatibility-warning-only until a later player-facing or tooling reason exists, such as editor object placement, object/pathing adoption, AI object valuation, renderer adoption, save-state adoption, or a scenario-authoring bundle.

## Decisions Needed

- Economy implementation will need an AcOrP decision on whether the internal `wood` resource id remains as a compatibility alias/displayed as Timber or migrates to canonical `timber` through a save-aware schema update.
- Economy implementation should also confirm whether the full nine-resource target in `docs/economy-overhaul-foundation.md` is acceptable for staged production, or whether the rare/faction resources should be grouped before JSON migration.
- Object implementation should confirm whether the density bands and object-class contract in `docs/overworld-object-taxonomy-density.md` are acceptable as the production target before schema/editor migration.
- Object implementation should review whether the hybrid neutral encounter representation in `docs/neutral-encounter-representation-plan.md`, report contract in `docs/neutral-encounter-additive-validator-report-plan.md`, and bundle metadata contract in `docs/neutral-encounter-representation-bundle-001-plan.md` are acceptable before broader production metadata migration.
- Object implementation should review `docs/neutral-encounter-first-class-object-migration-plan.md` before first-class object-backed migration and confirm that report/fixture scaffolding should come before any production object records.
- Object implementation should review `docs/neutral-encounter-first-class-object-bundle-001-plan.md` before implementation and confirm whether the proposed ids, lifted agreement, object-schema field set, and basalt guard-target convention are acceptable for a metadata-only production object-backed bundle.
- Object implementation should confirm when true footprint occupancy and approach-tile validation should become gameplay rules rather than renderer/editor hints.
- Object implementation should review whether the completed `safe_metadata_bundle_001` metadata shape is acceptable as the first migrated production bundle before any broader object migration.
- Magic implementation should confirm whether the seven-school accord model in `docs/magic-system-expansion-foundation.md` is the production target before spell schema, UI, AI, and content migration.
- Magic implementation should confirm how aggressively resource catalysts should be used for tier 3+ and adventure-map spells so the future economy model gains meaning without making spellcasting feel like bookkeeping.
- Magic implementation should confirm that Old Measure spells remain rare, scenario-gated, and route/object constrained rather than becoming a normal universal spell school.
- Magic implementation should confirm adventure-spell safety rules before any route bypass, site renewal, economy boost, or reveal spell is implemented in live maps.
- Artifact implementation should confirm the target equipment slot model in `docs/artifact-system-expansion-foundation.md`, especially the dedicated relic slot, two trinket slots, and whether mount/conveyance becomes a real slot or later optional layer.
- Artifact implementation should confirm whether unique sets should become normal skirmish rewards or remain mostly faction landmark, guarded site, Old Measure, and campaign-chain rewards.
- Artifact implementation should confirm how broadly curses/tradeoffs should appear in normal maps, since they add strategic texture but require UI, AI, save, and removal-service support before use.
- Artifact implementation should confirm that Old Measure artifacts remain rare, charged, attuned, or scenario-gated rather than becoming normal high-stat loot.
- Artifact implementation should confirm whether artifact economy hooks may touch rare resources before the economy resource-id migration and market-cap rules are implemented.
- Animation implementation should confirm whether the first vertical proof should prioritize battle readability or overworld/town state clarity.
- Animation implementation should confirm which faction should anchor the first motion proof, with Embercourt as the likely low-risk candidate because Beacon, roads, towns, and River Pass-style proof are already central.
- Animation implementation should confirm whether production units/heroes should target sprite sheets, cutout scene rigs, or a hybrid pipeline before asset production starts.
- Animation implementation should confirm normal-mode versus fast-mode pacing targets before battle and AI-turn playback are implemented.
- Animation implementation should confirm audio direction and cue priority rules before SFX production starts.
- Strategic AI implementation should confirm whether the current pressure/raid enemy model should migrate first into explicit AI event streams or into full AI hero roster/task state.
- Strategic AI implementation should confirm the intended fairness policy for hard difficulty: better planning and scouting inference are acceptable, but any resource, movement, visibility, or combat bonuses should be labelled.
- Strategic AI implementation should confirm which two factions should anchor the first AI personality proof; Embercourt versus Mireclaw is the likely low-risk pair because it contrasts infrastructure defense with raid/counter-capture pressure.
- Strategic AI implementation should confirm whether adventure-map spells that affect scouting, routes, economy, or site state are blocked until AI can evaluate them, as recommended in `docs/strategic-ai-foundation.md`.
- Concept-art curation should get AcOrP accept/reject/defer calls on the first external world mood plus six-faction identity batch recorded in `docs/concept-art-batch-001-review.md`.
- Concept-art curation should confirm whether the world mood direction is useful as broad Aurelion Reach infrastructure-fantasy mood, or whether the next pass should move immediately to cleaner region/readability frames.
- Concept-art curation should confirm whether Embercourt and Mireclaw can move into town/object second-pass briefs, while Sunvault, Thornwake, Brasshollow, and Veilmourn first need stronger anti-generic silhouette passes.
- Concept-art curation should explicitly reject embedded text, generated labels, pseudo-logos, and tagline copy from any future visual reference. The first Veilmourn sheet contains useful motifs, but its embedded labels/text make it non-approvable as-is.
- Concept-art curation should review the second-pass outputs recorded in `docs/concept-art-batch-002-review.md`: cleaner world route readability, no-text Veilmourn, and Sunvault silhouette direction.
- Concept-art curation should confirm whether the no-text Veilmourn revision is acceptable as material/prop direction, while still requiring town exterior and unit-ladder follow-up before implementation briefs.
- Concept-art curation should confirm whether the Sunvault second pass sufficiently reduces holy paladin drift for object/building language, or whether unit silhouettes need another stricter calibration-worker and relay-engineer pass.
- Concept-art curation should review the second-pass expansion outputs recorded in `docs/concept-art-batch-003-review.md`: Thornwake silhouette/town/object, Brasshollow silhouette/town/object, and Embercourt town/object readiness.
- Concept-art curation should confirm whether Embercourt town/object direction can move into compact implementation-brief drafting after remaining second-pass coverage is less lopsided.
- Concept-art curation should confirm whether Thornwake needs a stricter seven-tier unit ladder pass before unit approval, even though the root-gate, graft-nursery, toll-arch, and living-road object language is useful.
- Concept-art curation should confirm whether Brasshollow needs another anti-steampunk, contract-law silhouette pass focused on debt seals, clause tablets, repair windows, worker courts, and machine maintenance.
- Concept-art curation should review the second-pass expansion outputs recorded in `docs/concept-art-batch-004-review.md`: Mireclaw town/object, Veilmourn town/unit, and Sunvault seven-tier ladder.
- Concept-art curation should confirm whether Mireclaw town/object direction is strong enough for compact overworld town and object-family brief drafting, while keeping unit-ladder approval deferred.
- Concept-art curation should confirm whether Veilmourn harbor/town direction is accepted and whether the unit ladder needs a stricter tier-separation pass before combat briefs.
- Concept-art curation should confirm whether Sunvault seven-tier direction is sufficient to close the anti-paladin recalibration loop, or whether another black-shape-only unit pass is needed.
- Concept-art curation should review the broader overworld object/town-adjacent outputs recorded in `docs/concept-art-batch-005-review.md`: route-law/transit objects, persistent resource fronts, neutral encounters, guarded rewards, pickups, and faction landmarks.
- Concept-art curation should confirm whether the route-law/transit direction is acceptable for brief drafting, especially tollhouses, ferries, root gates, rail switches, prism markers, bell markers, and mirror/fog markers.
- Concept-art curation should confirm whether persistent resource-front families are distinct enough for staged economy-site briefs, especially ore quarry, aetherglass orchard, peatwax cut, embergrain yard, verdant graft nursery, furnace/contract site, memory-salt/salvage field, and timber/rail yard.
- Concept-art curation should review the hybrid neutral encounter target: visible army objects are the default, camps are explicit fixed-site subtypes, and guard-linked variants must keep both guard and target readable.
- Concept-art curation should review the first implementation-brief prep pass in `docs/concept-art-implementation-briefs.md`, especially whether Embercourt town/object, Mireclaw town/object, route-law/transit, persistent resource fronts, guarded rewards, pickups, faction landmarks, and neutral encounter presentation are acceptable planning translations while final art approval remains open.
- Concept-art curation should confirm whether the brief rejection constraints are strict enough: generated PNGs are not source assets; exact layouts, silhouettes, banners, pseudo-heraldry, icons, labels, color chips, and object arrangements must not be copied into runtime production.
- Object schema planning should confirm whether `docs/concept-art-implementation-briefs.md` is acceptable as the source for first schema fields: primary object class, secondary tags, footprints/body tiles, approach offsets, passability, interaction cadence, guard links, reward categories, ownership/capture states, route effects, and animation cue ids.
- Object schema migration should review `docs/overworld-object-schema-migration-plan.md` before implementation and confirm the proposed `primary_class` values: `decoration`, `pickup`, `interactable_site`, `persistent_economy_site`, `transit_route_object`, `neutral_dwelling`, `neutral_encounter`, `guarded_reward_site`, `faction_landmark`, and `scenario_objective`.
- Object schema migration should confirm that current `family`, `passable`, `visitable`, and positive `footprint` fields remain compatibility fields through the first additive schema, validator-warning, fixture, renderer/editor, pathing, and AI adoption stages.
- Object schema migration should confirm whether true body-tile occupancy and approach offsets stay editor/validator metadata until migrated maps pass placement checks, instead of immediately changing pathing behavior.
- Object schema migration should confirm the validator/report rollout for first-class neutral encounter representation: report-only compatibility inference first, fixture-only strictness second, and production metadata bundle only after review.
- Object schema migration should confirm whether route effects may be authored as metadata-only until pathing and strategic AI can safely evaluate linked endpoints, movement discounts/taxes, fog bypass, repair unlocks, and scenario gates.
- Object schema migration should confirm whether animation cue ids can be referenced before a cue catalog exists, with warnings rather than hard validation.
- Economy/resource schema migration should now resolve resource ids, `wood`/`timber` compatibility, output cadence, persistent-site capture values, market/exchange caps, faction preferences, AI valuation hooks, and save/schema compatibility before persistent resource-front content migration.
- Economy/resource schema migration should review `docs/economy-resource-schema-migration-plan.md` before implementation and confirm that the first live slice should be additive schema fields plus validator warning/report planning, not production JSON migration.
- Economy/resource schema migration should confirm whether `experience` stays a progression reward key outside the stockpile resource registry.
- Economy/resource schema migration should confirm the initial resource registry fields: `id`, `display_name`, `category`, `market_tier`, `default_visible`, `legacy_aliases`, `faction_affinity`, `ui_sort`, `icon_hint_id`, and `material_cue`.
- Economy/resource schema migration should confirm the output cadence vocabulary: `instant_claim`, `daily`, `weekly`, `limited_charges`, `route_linked`, `service_refresh`, `battle_cleanup`, and `scenario_scripted`.
- Economy/resource schema migration should confirm persistent-site capture profile fields for counter-capture value, pillage policy, damaged-state policy, retake recovery days, defense investments, and route dependency tags.
- Economy/resource schema migration should confirm that normal markets remain common-resource smoothing first, with no broad rare-resource buying until weekly caps, source discovery, UI display, AI use, and save state are implemented.
- Economy/resource schema migration should confirm that faction resource preferences are advisory AI/content weights first, not hard locks or private currencies.
- Economy/resource schema migration should confirm that resource-site behavior remains in `resource_sites.json` while map-object schema owns presentation, footprint, approach, passability, and visible state metadata.
- Economy/resource additive validator planning should review `docs/economy-resource-additive-schema-validator-plan.md` before implementation and confirm that the first validator/report slice is opt-in, report-only for production content, and strict only for sample fixtures or declared migrated bundles.
- Economy/resource additive validator planning should confirm whether the first resource registry lives only in tests/report fixtures before any later `content/resources.json` production content addition.
- Economy/resource additive validator planning should confirm whether default `python3 tests/validate_repo.py` output may include compact economy warning counts or whether economy reports should remain behind explicit CLI flags.
- Economy/resource additive validator planning should confirm that `wood` remains the live id and Timber display/alias reporting stays advisory until a save-aware canonical-id decision is made.
- Economy/resource additive validator planning should confirm that missing output cadence, capture profile, and market caps are compatibility warnings only, not production-content errors, until a migrated bundle declares the strict schema.
- Economy/resource additive validator report review should decide whether the opt-in report's compatibility warning volume is acceptable for the next migration planning pass, especially capture-profile warnings on current persistent sites and legacy market-cap warnings.
- Economy/resource additive validator report review should confirm that strict economy fixtures may remain isolated under `tests/fixtures/economy_resource_schema/` until a later explicit `content/resources.json` production registry slice.
- Overworld object additive validator report review should decide whether the opt-in report's warning volume is acceptable for the next migration planning pass, especially missing body tiles, approach metadata, passability class, interaction metadata, animation cue ids, editor placement hints, and AI hints across current map objects.
- Overworld object additive validator report review should decide whether placed resource sites without map-object links should stay compatibility-only for now or become first production additive schema targets.
- Overworld object additive validator report review should confirm whether strict overworld object fixtures may remain isolated under `tests/fixtures/overworld_object_schema/` until a later explicit production object-schema migration slice.
- Overworld object additive validator report follow-up has confirmed additive neutral encounter report fields and fixture scope in `docs/neutral-encounter-additive-validator-report-plan.md`; production object JSON migration remains blocked until opt-in report/fixture implementation and review.
- Overworld object additive validator report review should confirm that transit route-effect metadata, animation cue ids, and AI/editor hints remain report-only placeholders until pathing, animation catalog, and strategic AI adoption slices are approved.

## Current Follow-Up

- Concept-art review notes are recorded in `docs/concept-art-batch-001-review.md`, `docs/concept-art-batch-002-review.md`, `docs/concept-art-batch-003-review.md`, `docs/concept-art-batch-004-review.md`, and `docs/concept-art-batch-005-review.md`; all generated PNGs remain external only.
- The first implementation-brief prep pass is recorded in `docs/concept-art-implementation-briefs.md`. It is planning evidence, not final asset approval. AcOrP review remains open and is not a hard blocker to object schema migration planning.
- The object schema migration planning contract is recorded in `docs/overworld-object-schema-migration-plan.md`. It is planning evidence only; no runtime code, scenario placement behavior, renderer sprite ingestion, runtime asset import, pathing change, or AI adoption has happened yet.
- The economy/resource schema migration planning contract is recorded in `docs/economy-resource-schema-migration-plan.md`. It is planning evidence only; no production JSON migration, runtime economy behavior change, market implementation, save migration, renderer sprite ingestion, or resource-site bundle migration has happened yet.
- The first additive resource schema and validator warning/report planning contract is recorded in `docs/economy-resource-additive-schema-validator-plan.md`.
- The first additive resource validator/report scaffolding implementation is complete locally as opt-in `tests/validate_repo.py` report support plus non-production strict fixtures. It adds no production JSON migration, market/runtime behavior change, save migration, rare-resource activation, renderer sprite ingestion, runtime asset import, or generated concept-art import.
- Additive overworld object validator/report scaffolding is complete locally as opt-in `tests/validate_repo.py` report support plus non-production strict fixtures. It adds no production JSON migration, renderer sprite ingestion, runtime asset import, pathing/body-tile/occupancy behavior change, visit/interaction runtime change, or AI adoption.
- Additive overworld object report review/follow-up, safe additive schema planning, `safe_metadata_bundle_001` implementation, neutral encounter representation planning, neutral encounter additive validator/report planning, neutral encounter additive report review, and neutral encounter bundle planning are complete locally.
- Neutral encounter representation planning is complete locally in `docs/neutral-encounter-representation-plan.md`, additive neutral encounter validator/report planning is complete locally in `docs/neutral-encounter-additive-validator-report-plan.md`, neutral encounter report/fixture scaffolding is complete locally in `tests/validate_repo.py` plus `tests/fixtures/neutral_encounter_schema/`, report review is complete in `docs/neutral-encounter-report-review-001.md`, bundle planning is complete in `docs/neutral-encounter-representation-bundle-001-plan.md`, metadata-only bundle implementation is complete locally, authored bundle report review is complete in `docs/neutral-encounter-representation-bundle-001-report-review.md`, first-class object migration-boundary planning is complete in `docs/neutral-encounter-first-class-object-migration-plan.md`, first-class object report/fixture scaffolding is complete locally, first-class object report review is complete in `docs/neutral-encounter-first-class-object-report-review.md`, tiny object-backed bundle planning is complete in `docs/neutral-encounter-first-class-object-bundle-001-plan.md`, metadata-only object-backed bundle implementation is complete locally, and implemented object-backed bundle report review is complete in `docs/neutral-encounter-first-class-object-bundle-001-report-review.md`. Neutral encounter migration is paused, not the next current companion slice.
- Foundation/game-loop prioritization is complete locally in `docs/foundation-game-loop-prioritization.md`. The proof planning, current-systems proof, manual gate review, strategic AI economy pressure planning, first pressure implementation, report gate review, event surfacing planning, event surfacing implementation, event surfacing report gate, strategic AI pressure expansion planning, town governor pressure report implementation, town governor pressure report gate, faction personality pressure planning, faction personality evidence report, strategy-config audit planning, strategy-config audit report, capture/counter-capture/defense proof planning, site-control proof report, Glassroad defense proof planning, Glassroad defense proof report, commander-role state boundary planning, commander-role report fixture planning, minimal commander-role state schema planning, commander-role report implementation boundary planning, commander-role report implementation, commander-role report gate review, commander-role adoption sequencing, commander-role live-turn transcript/report planning, commander-role live-turn transcript/report implementation, and commander-role live-turn transcript/report gate review follow-ups are also complete, so the next current slice is `strategic-ai-hero-task-state-boundary-planning-10184`, not more neutral encounter metadata migration, schema adoption, live commander-role behavior, full AI hero task-state implementation, or coefficient tuning.
- Economy capture/resource loop proof planning is complete locally in `docs/economy-capture-resource-loop-proof-plan.md`, the current-systems proof report is complete in `docs/economy-capture-resource-loop-live-proof-report.md`, and the manual gate review is complete in `docs/economy-capture-resource-loop-manual-gate-review.md`. The economy gate passed; exact routed live-client transcript coverage is deferred unless explicitly requested.
- Strategic AI economy pressure planning is complete locally in `docs/strategic-ai-economy-pressure-slice-plan.md`, the first implementation is complete in `docs/strategic-ai-economy-pressure-implementation-report.md` plus `EnemyAdventureRules` score/report helpers and focused Godot report coverage, and the report gate passes in `docs/strategic-ai-economy-pressure-report-gate-review.md`. Compact event/threat reason surfacing planning, implementation, and report gate review are complete in `docs/strategic-ai-event-surfacing-plan.md`, `docs/strategic-ai-event-surfacing-implementation-report.md`, and `docs/strategic-ai-event-surfacing-report-gate-review.md`. Strategic AI pressure expansion planning, town governor pressure report implementation, town governor pressure report gate review, faction personality pressure planning, faction personality evidence reporting, strategy-config audit planning, strategy-config audit reporting, capture/counter-capture/defense proof planning, site-control proof reporting, Glassroad defense proof planning, Glassroad defense proof reporting, commander-role state boundary planning, commander-role report fixture planning, minimal commander-role state schema planning, commander-role report implementation boundary planning, commander-role report implementation, commander-role report gate review, commander-role adoption sequencing, live-turn transcript/report planning, live-turn transcript/report implementation, and live-turn transcript/report gate review are complete in `docs/strategic-ai-pressure-expansion-plan.md`, `docs/strategic-ai-town-governor-pressure-implementation-report.md`, `docs/strategic-ai-town-governor-pressure-report-gate-review.md`, `docs/strategic-ai-faction-personality-pressure-plan.md`, `docs/strategic-ai-faction-personality-evidence-report.md`, `docs/strategic-ai-strategy-config-audit-plan.md`, `docs/strategic-ai-strategy-config-audit-report.md`, `docs/strategic-ai-capture-countercapture-defense-proof-plan.md`, `docs/strategic-ai-capture-countercapture-defense-proof-report.md`, `docs/strategic-ai-glassroad-defense-proof-plan.md`, `docs/strategic-ai-glassroad-defense-proof-report.md`, `docs/strategic-ai-commander-role-state-plan.md`, `docs/strategic-ai-commander-role-report-fixture-plan.md`, `docs/strategic-ai-minimal-commander-role-state-schema-plan.md`, `docs/strategic-ai-commander-role-report-implementation-plan.md`, `docs/strategic-ai-commander-role-report-implementation-report.md`, `docs/strategic-ai-commander-role-report-gate-review.md`, `docs/strategic-ai-commander-role-adoption-sequencing-plan.md`, `docs/strategic-ai-commander-role-live-turn-transcript-report-plan.md`, `docs/strategic-ai-commander-role-live-turn-transcript-report-implementation-report.md`, and `docs/strategic-ai-commander-role-live-turn-transcript-report-gate-review.md`; the next current slice is planning-only strategic AI hero task-state boundary work, not coefficient tuning, full AI hero implementation, durable event logs, defense-specific durable state, production JSON migration, schema writes, live behavior adoption, or broader AI architecture.
- GitHub push/auth remains blocked by invalid credentials; keep work local and do not push until credentials are refreshed.
