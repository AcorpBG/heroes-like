# Zenith Lensbearers reciprocal H3 walk

Published movement uses guided_v2 source frames 40-78 every two frames: 20 observed poses at 70 ms, a 1400 ms cycle retimed from approximately 1667 ms. The round near knee leads at 40-48, passes at 52-56, trails at 60-72, then returns at 78-80. The smaller far knee leads at 60-68. Both hands retain separate grips on one rigid lens gun; the back orb and prism plates remain attached.

The first H3 take (seed 2026092424) was rejected for repeatedly leading with the far leg. Preserve its original video, exact workflow/prompt, source-guide lineage and rejection. Built-in image generation then supplied explicit opposite contact poses; key_guides/generation.json records originals, exact prompts, references and hashes. Both contacts retain the same upper body and equipment. Source alpha was inspected before compositing; dark invisible RGB is not an opaque background.

guided_v2 (seed 2026092425) uses identical near-contact first/last guides plus far contact at frame 62 through MiniMaxH3AddGuide. Output is 960x544, 124 frames at 24 fps, 20 res_multistep/simple steps; generation took 121.969 seconds. The model produced repeated reciprocal cycles despite the one-cycle prompt; the selected interval is one observed complete cycle. Runtime source scale .158 matches the original approximately 220 px body height; input scale .265 and extraction .158/.265, fixed anatomical anchor [480,475]. No per-frame bounds normalization or invented intermediate poses.

Coordinator inspected all 124 chronological frames, enlarged light/dark anatomy and alpha, the 78/80/40 loop boundary, and actual Godot battle-scale output. Windows candidate checks passed 132 assertions; published checks passed 146. Ordered-frame/render/runtime review is distinct from continuous browser playback, unavailable through the policy-blocked preview route; no bypass, manual game playtest, Linux run or full suite is claimed. Owner explicitly directs autonomous coordinator review and publication without per-creature approval questions.

All 48 earlier poses across seven accepted actions retain exact pixels, offsets and timings. Map idle PNG is byte-identical, other unit rows are unchanged, and candidate/live atlas hashes match. FFV1 retains every decoded RGB frame; original.json records exact hashes. produce.py process rebuilds all mattes, review rebuilds chronological contacts, and build rebuilds handoffs (pending by default until reviewed). Preserve both source videos, original art/guides/provenance and the 20 selected RGBA frames. Remove only rebuildable unused extractions/reviews/logs and hash-verified duplicate server exports.
