# Millstone Slingers: underarm ranged release

Published `ranged` for `unit_neutral_millstone_slingers` from `underarm_v2`: 39 observed frames, 12-88 every 2, at 50ms per pose (1950ms). Source frame48, selected index18, is the cord release; projectile travel is300ms and remains runtime-owned. The original interval spans approximately3250ms and is deliberately retimed. No synthetic, reversed or duplicated poses.

The previous overhead attempts distorted the sling. Three new original transparent key paintings establish a low backswing, intermediate low swing and empty-cup release. Built-in image_gen originals, exact prompts, reference/output paths and hashes are under `key_guides`. H3 guides use ready endpoints, backswing26, swing42, release54 and ready92. One anatomical scale per painting (.58 legacy ready, .195 new keys) is divided by .6 for video input; output uses fixed .6 at anchor480,475. The figure widens its stance slightly during preparation and returns during recovery; no per-frame alignment or size normalization hides that movement.

The selected video has visible arm articulation through the underarm release, followed by an upward empty-sling follow-through and return to ready. Autonomous review covered all124 chronological frames, enlarged release/grip/limb continuity, dark/light alpha and native128px Godot rendering. `underarm_v1` is rejected: frames44-45 duplicate the cup into two paddle-like shapes. The added intermediate guide removes that defect in v2. This is ordered-frame/native-render/runtime review, not continuous browser playback, a manual playtest or Linux execution.

Both original124-frame960x544/24fps FFV1 and MP4 pairs, decoded RGB hashes, exact workflows/prompts/guides and job histories remain. Local models: MiniMax H3 int8_convrot, Qwen3VL32B nvfp4_awq and H3 video VAE int8_convrot;20 res_multistep/simple steps. Seeds2026092501/02, generation127.724/129.194 seconds. Plain verified-magenta alpha unmix and edge-only despill preserve original geometry. The39 selected RGBA frames remain.

Candidate220 and published234 focused Windows checks passed, including Normal/Fast/reduced-motion. Candidate/live atlas hashes match. All115 previously accepted poses retain exact pixels, offsets and timing. Overworld idle PNG is byte-identical; its source crop/hash metadata follows the repacked battle atlas. Other creature catalog/map rows are unchanged. All eight required Millstone actions are now accepted. Roster:107 complete,0 partial,125 without accepted coverage; the overall goal remains active.

Rebuild a take with `produce.py process`, review with `review`, and build its selected pending candidate with `build`. Building resets visual acceptance intentionally. Explicit publication uses `tools/publish_fluid_creature_animation.py` with only `ranged` after autonomous review. Shared original sources live in the previous `millstone_h3` and batch_d folders; do not delete them.

Cleanup removed494 exact task-owned disposable files (162539742 bytes,155.0MiB): previews, native logs/test profiles/exports, unselected mattes and hash-verified duplicate Comfy PNG/MP4/input exports. They are rebuildable from retained originals/tooling. Caches, original art/video/provenance, selected frames, saves, backups and unrelated files remain. Queue was empty, no Godot process remained, and task-loaded models were unloaded while ComfyUI stayed available.
