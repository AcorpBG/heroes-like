# Ashdart Stalkers H3 reciprocal movement

`unit_neutral_ashdart_stalkers`: 31 observed source frames 2–62 every second frame, at 55 ms each (1,705 ms). Both legs exchange contact, support and passing roles while the original blowpipe remains rigidly held in both hands. The orange scarf/cape and black ponytail follow the steps. The roughly 2,583 ms source cycle is deliberately accelerated for travel; no reverse, duplicate padding, interpolation or whole-body warping is used.

Earlier `move_v1` and `move_return_v1` sheets repeatedly lead with the same leg and remain unpublished. This video instead uses the existing accepted `attack_v1_0` ready painting as first and last guide, with no misleading same-leg midpoint. `reference.json` preserves its exact source rectangle, alpha threshold, anatomical anchor and hashes. Input scale is 1.0 on a 960×544 canvas at anchor (480,475); extraction is a fixed 0.54, matching the accepted ready body. Pose bounds never determine individual scale or alignment.

The local MiniMax H3 graph used seed 2026092415, 20 steps, 124 frames at 24 fps and a flat pure-green background. Generation took approximately 136 seconds. The FFV1 original preserves every decoded RGB frame; original MP4, prompt, workflow, guides, hashes and server history are retained. `produce.py process` reconstructs all keyed frames with corner-color validation, green unmix/despill and opaque-connected thin-edge retention. `review` rebuilds temporary chronological sheets; `build` rebuilds the candidate handoff. Do not resubmit or overwrite originals.

Review covered all 124 chronological frames, enlarged light/dark alpha/anatomy samples and the native Godot battle-scale overview. Continuous browser playback could not be checked: the browser URL policy blocked the local preview page, and no workaround was attempted. No continuous playback, manual game playtest, Linux execution or full-suite validation is claimed. The existing seven actions preserve all 48 accepted poses with identical pixels, offsets and timing; overworld idle is byte-identical. Published acceptance and focused validation are recorded in the runtime provenance/tracker. The source handoff remains a pending generation recipe.

Retain the 31 selected RGBA frames and original generation/source metadata. Unselected extractions, temporary review/render/import outputs and hash-verified duplicate ComfyUI exports are rebuildable and removed after delivery.
