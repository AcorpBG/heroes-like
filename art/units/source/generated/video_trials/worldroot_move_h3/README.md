# Worldroot Bastion H3 movement

Selected movement for `unit_thornwake_worldroot_bastion`: 31 observed video frames, source 0–120 every fourth frame, at 50 ms each (1550 ms loop). Each of the four root feet lifts and plants in sequence while the others support the living fortress. Its six earlier accepted actions retain all 40 packed poses, timing and anatomical offsets; the overworld idle image remains byte-identical.

Four existing original paintings in the unit's `packing.json` supply the guides: `step_rear_left` at both endpoints, `step_fore_left` at frame 30, `step_rear_middle` at 61 and `step_fore_right` at 92. Their exact rectangles, original anchors and hashes are recorded in `reference.json`. Every guide uses source scale 0.36 on a 960×544 canvas with anchor (480, 465). Every extracted frame uses the original runtime walking scale 0.185 divided by 0.36. No bounding-box normalization, per-frame shifting, warping, synthetic interpolation or reversed poses is used. Magenta keying preserves the green doorway, foliage, ivory birch and red/amber ornaments.

Local MiniMax H3 generated 124 frames at 24 fps with 20 `res_multistep` steps, seed 2026092410. Generation took 162.908 seconds. The exact submitted graph, prompt, uploaded guide names, model filenames, prompt ID and generation history remain alongside the original FFV1 video and MP4. The graph chains all three intermediate guides into the sampler conditioning while retaining the original latent connection. All 124 decoded RGB frames match the original server PNG hashes.

Review covered all chronological source frames, enlarged root-leg and architecture details on light/dark backgrounds, and native Godot battle-size phases against accepted idle. The four roots retain their attachments and lift/plant individually; the towers, crown, green doorway and hanging pennants remain consistent. The selected return pose is visually near-matched, supported by a premultiplied loop difference of about 0.035 versus 0.124 median adjacent-phase difference. No continuous manual game playtest or Linux execution is claimed.

`produce.py process` reconstructs the matte from the preserved FFV1; `review` reconstructs chronological sheets under `.artifacts/worldroot_move_h3`; `build` reconstructs the selected handoff and combined candidate. Python requires Pillow, NumPy, SciPy and PyAV. Matting checks a uniform magenta plate, unmixes its color, removes magenta RGB spill and preserves separate alpha components containing opaque pixels. Existing submitted originals cannot be overwritten or accidentally submitted twice.

Focused Windows validation passed 127 candidate and 141 published assertions after a successful headless import. Candidate and published atlas files are byte-identical. Source originals, exact prompts, workflows, provenance and the 31 selected RGBA frames are retained. Unselected extractions, duplicate server exports and temporary review/test outputs are rebuildable and removed after verification. No full repository suite is run.
