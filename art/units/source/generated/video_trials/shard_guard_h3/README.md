# Shard Guard H3 idle and reciprocal walk

Published idle uses 32 observed frames 0-62 every two frames at 83 ms (2656 ms): visible sword elbow/wrist ready adjustment, brief hold and return, with planted boots and retained shield grip. Published movement uses 20 observed frames 44-82 every two frames at 60 ms (1200 ms, retimed from a 1667 ms source cycle). Near leg leads44-52, passes56, trails60-72 while far leg leads, passes76 and returns forward80-84. Shoulder counter-rotation and cloth follow the stride; the complete crystal sword, shield, crown and armor remain attached.

Both original videos are 960x544,124frames at24fps,20res_multistep/simple steps on uniform green. Idle seed2026092431 completed in117.479seconds; movement seed2026092432 in113.780seconds. Original accepted attack-ready painting supplies idle at fixed .61 runtime scale (.95 input, .61/.95 extraction). An original built-in image edit supplies the explicit right-facing walking contact, matching body height at .172 runtime scale (.28 input, .172/.28 extraction). Anatomical anchors map to480,475, without per-frame bounds normalization. key_guides/generation.json records exact prompts and image/reference hashes. identity_reference.png retains the original-pixel crop used for the edit.

All124 frames of each take were reviewed chronologically, followed by enlarged light/dark anatomy and alpha, passing poses, idle62/64/0 and move82/84/44 loop boundaries, and native Godot battle/overworld-scale output. Candidate150 and live154 focused Windows checks passed, including Normal/Fast/reduced-motion.32 original poses across five accepted combat actions preserve exact pixels, offsets and timings. Other creature manifest/map rows remain unchanged; the new32-frame map idle matches the battle idle. Candidate/live atlas hashes match.

This was ordered-frame, native-render and runtime review, not continuous browser playback (the preview route was policy-blocked). No bypass, manual playtest, Linux execution or full-suite claim. Coordinator owns review and publication, per owner direction; no routine user approval is required.

Preserve both original FFV1 videos and MP4s, source guides, prompts, workflows, provenance and52 selected RGBA frames. original.json records every decoded RGB hash. Each produce.py process rebuilds mattes; review rebuilds chronological contacts; build rebuilds the clip handoff. The root handoff combines idle and move; publish_fluid_creature_animation.py merges them with the existing reviewed combat recipes. Temporary previews, logs, test profiles, unused mattes and hash-verified duplicate server exports were removed (approximately131.9MiB); all original source and rebuild tooling remain.
