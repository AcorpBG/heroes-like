# Emberpack Lobbers reciprocal H3 walk

Movement replaces only the repeated-leading-foot fallback. Selected facing_v2 frames32-94 every2 retain32 observed poses at55ms,1760ms per loop, accelerated from approximately2667ms source gait. Near buckled boot and far boot each reach forward, plant, pass and trail. One gathered sling stays connected and clear of feet; one ember basket and brass flask stay strapped to the back.

The first124-frame H3 take was rejected for rotating into the opposite travel direction. The original idle guide, all original RGB video pixels, MP4, exact prompt/workflow, seed2026092422 and rejection reason remain. Original video generation took119.768seconds.

Built-in image generation supplied corrected right-facing key art. contacts_v1 was rejected as an opposed-contact pair because both figures lead with the same leg. The dark preview is invisible RGB behind valid alpha, not an opaque background (verified after channel inspection). right_contact_v2 is the selected single guide. key_guides/generation.json records exact tool inputs/outputs, prompts and hashes. The generated guide retains the design and gathers the sling above the boots. Source scale .195 retains roughly180px body height matching the original .4-scale painting; input .45, extraction .195/.45 and fixed anatomical anchor480,475. No per-frame size/bounds normalization.

facing_v2 used seed2026092423,960x544,124frames24fps,20steps,uniform green, identical first/last guide. Generation took116.581seconds. FFV1 preserves every decoded RGB frame; exact server-frame hashes retained in original.json. produce.py process recreates extracted RGBA; review recreates chronological contacts; build recreates handoffs. Never resubmit/overwrite an existing job.

Coordinator reviewed all124 frames in chronological order, enlarged light/dark anatomy and alpha, explicit loop seam32/92/94/96, and native Godot battle-scale output. Candidate integrity checks passed156 assertions. Ordered-frame/native-render/runtime review is distinct from continuous browser playback, which remains unavailable through the policy-blocked local preview route; no bypass or manual game playtest is claimed. Owner explicitly directed autonomous coordinator review/publication without per-creature approvals. No Linux run or full suite.

All48 earlier poses across seven accepted actions retain exact pixels, offsets and timings. Map idle PNG is byte-identical; all other unit rows unchanged. Candidate and live atlas hashes match. Preserve source videos/art/provenance and32 selected RGBA files; discard only rebuildable extractions/reviews/logs and verified duplicate server exports.

Published Windows validation:160 focused checks passed after import.
