# Reefbolt Crews H3 transport movement

`unit_neutral_reefbolt_crews`:20 observed source frames40–78 every second frame at65ms each, a1300ms two-step carrying cycle. Both humans alternate support/passing boots while maintaining four load-bearing hands. The man holds the crossbow stock, the woman lifts the tripod beam, the basket stays attached, and all three rigid tripod legs remain clear of the ground. Runtime tempo deliberately shortens the roughly1667ms source cycle; no duplicate, reverse or interpolated poses are added.

Existing original `crew_carry_passing` and `crew_carry_opposite_attempt` paintings from the unit's `packing.json` supply endpoints and guide61. Exact source rectangles, anatomical anchors and hashes are retained in `reference.json`. The input canvas is960x544 with anchor(480,480), input scale.42 and extraction.225/.42, preserving the original walking scale. There is no per-frame bounding-box normalization, stabilization or geometry editing. No new image-generation artwork was needed.

Local MiniMax H3 generated124 frames at24fps with20res_multistep/simple steps, seed2026092413, prompt ID0cd551bd-fffc-485f-9b84-fd8d52eb0dd7. Generation took130.555s. Exact prompt, graph, model names, guide/upload hashes and history remain with original FFV1 and MP4. All124 decoded RGB hashes match the original server frames. The magenta background remained uniform; chroma unmix/despill and alpha-component filtering preserve fine ropes, grips and blade tips.

Review covered every chronological frame, enlarged human anatomy/weapon/alpha details against light and dark backgrounds, and actual Godot battle-size phases against accepted idle. Candidate144 and published148 focused Windows checks passed after headless import. Candidate/live atlas is byte-identical. All48 poses in seven earlier accepted actions retain exact pixels, offsets and timing; overworld idle is byte-identical. No full suite, continuous manual game playtest or Linux execution is claimed.

Rebuild with `produce.py process`, `review`, then `build` using Python with Pillow, NumPy, SciPy, PyAV and repository packing tools. Source originals, provenance and20 published RGBA frames are retained. Unselected extractions, temporary review renders/logs/import profile and hash-verified duplicate server exports are disposable and rebuildable. Do not resubmit or overwrite the original job.
