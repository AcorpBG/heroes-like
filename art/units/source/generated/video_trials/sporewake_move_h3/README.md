# Sporewake Chanters H3 movement

Published for `unit_mireclaw_sporewake_chanters`: 23 observed poses, source frames 0–44 every second frame, at 42 ms each (966 ms loop). This completes all eight required actions for this ranged creature. The seven earlier accepted actions retain all 48 original packed poses, timing and anatomical offsets; overworld idle remains byte-identical.

The existing original `step_right` and `step_left` paintings from `art/animation/source/poses/unit_mireclaw_sporewake_chanters/packing.json` guide the endpoints and midpoint. Both retain their original source anchor (645, 1360). A single guide scale of 0.33 places them on a 960×544 canvas at (480, 490); extracted frames use the original walk scale 0.158 divided by 0.33. No per-frame normalization, repositioning, warping, interpolation or reversed frames is used. Purple cloth requires cyan keying instead of the previous unit's magenta key.

Local MiniMax H3 generated 124 frames at 24 fps with 20 `res_multistep` steps, seed 2026092407, matched first/last images and an original opposite-step guide at frame 61. The actual API graph, input hashes, prompt ID, model filenames and generation history are preserved. Generation took 136.048 seconds. The video produced three gait repetitions despite the one-cycle prompt; only the first complete cycle is selected. Original FFV1 decoded pixels and the server MP4 remain intact.

Review covered all 124 chronological source frames, enlarged anatomy and alpha against light/dark backgrounds, and actual Godot battle-scale phases against accepted idle. Both boots alternate contact and bent-knee passing. The rigid antler staff, grip, masked face and single suspended three-chain censer remain coherent. The selected loop seam has premultiplied difference about 0.038 versus 0.102 median adjacent-phase difference; this supports the visual review rather than replacing it. The full original video and a reconstructed animation can be played for further review; no continuous manual game playtest is claimed.

`produce.py process` rebuilds every matte from the preserved original. The matte checks corner uniformity and cyan identity, unmixes the key color, removes cyan RGB spill, and retains separate alpha components containing opaque pixels. It does not erase detached ornaments through largest-component filtering. `produce.py review` rebuilds chronological previews under `.artifacts/sporewake_move_h3`. `produce.py build` rebuilds the selected handoff and merged candidate. Requires Python with Pillow, NumPy, SciPy and PyAV. Submission is guarded against duplicate jobs and overwriting originals.

Validation: 147 focused candidate assertions, successful headless Godot import, and 151 published assertions. Candidate and published atlases are byte-identical; all 124 lossless original frames match captured server PNG hashes. No full suite or Linux execution. Original art, videos, prompts, graph and the 23 selected RGBA sources remain. Other extractions, duplicate server exports and temporary review/test output are rebuildable and removed after verification.
