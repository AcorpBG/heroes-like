# Dustjack Blades reciprocal H3 walk

`unit_neutral_dustjack_blades` now has a reviewed 20-frame movement cycle, observed source frames40–78 every second frame from `blue_v2/original_lossless.mkv`, at60ms each (1200ms). The raw cycle spans about1667ms at24fps; the deliberate gameplay tempo is faster. Both boots alternate through contact, passing, support and toe-off, while two hands retain two original knives. Goggles, face, shoulder pauldron, scarf and body proportions remain consistent.

Two original opposite-leg contact paintings from `art/units/source/generated/fluid_animation/batch_d/unit_neutral_dustjack_blades/move_contacts_v1.png` guide endpoints and frame61. Exact original rectangles, anchors and hashes are in each attempt's reference.json. Both guides use.48 scale on960x544 at anchor(480,486). Runtime extraction uses.20/.48, matching the accepted idle's.40 scale from half-resolution paintings. There is no per-frame normalization, stabilization, warping, reversal or interpolation.

The initial attempt produced reciprocal movement but changed its magenta background to lime. Native-size review rejected its yellow-green edge halos even though its candidate passed128 structural checks. It was never published. Its original lossless video, MP4, prompt, graph, seed2026092411, source/matte hashes and rejected recipe remain. Rebuild its temporary frames with `produce.py process`.

The replacement in `blue_v2` uses a uniform deep-blue backdrop and seed2026092412. The generated blue plate remained safe across all124 frames. Matting uses measured uniform corner color, blue dominance unmix/despill, a background-color distance floor12, and connected alpha>=8 components containing opaque pixels>=128. This preserves fine knife tips and scarf edges without erasing anatomy. No new image-generation artwork was needed.

Both attempts used local MiniMax H3,124 frames at24fps,960x544,20res_multistep/simple steps. Exact graph, model names, prompts, seeds, guide/upload provenance, prompt IDs, generation history and all decoded RGB hashes are retained. Initial generation took125.225s; blue-v2 took151.002s. All248 original decoded frames were hash-verified against the server originals.

Review covered all chronological frames, enlarged face/grips/boots and alpha edges against light/dark, loop return poses, and actual Godot battle-size phase output against accepted idle. Blue-v2 passed128 candidate and132 published focused Windows checks after successful headless import. Candidate/live atlas is byte-identical. Six earlier actions retain all40 poses, exact pixels, offsets and timing; overworld idle is byte-identical. No full suite, continuous manual game playtest or Linux execution is claimed.

The game delivery is only `blue_v2/handoff.json`. `blue_v2/produce.py process` rebuilds all keyed frames from the original FFV1; `review` rebuilds chronological sheets; `build` rebuilds the pending candidate/handoff. Dependencies: Pillow, NumPy, SciPy, PyAV and existing repository packing tools. Keep source originals and the20 published RGBA frames. Other extractions, native review images/logs/profiles and verified duplicate server exports are disposable and rebuildable.
