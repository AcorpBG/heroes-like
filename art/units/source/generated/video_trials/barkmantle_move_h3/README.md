# Barkmantle Rams H3 movement

The selected movement is in `planted_v2`: 31 observed frames, source indices 0–120 every fourth frame, 42 ms each (1302 ms cycle). It adds alternating diagonal root-leg movement while retaining the original horned wooden beast. No new creature design, renderer behavior or gameplay rule is introduced.

The first video in this directory is rejected for overextension and growth of the near front paw, which approaches the lower canvas edge. The last opaque row is not actually clipped. Keep its original video, exact graph, prompt and review record. It is not accepted because it has many frames or passes a structural check.

The correction uses only original painted references: `walk_near_diagonal` at the endpoints, `walk_far_diagonal` at frame 61, and `idle_0` as an all-four-paws planted guide at frames 30 and 92. References and original source hashes are recorded in `planted_v2/reference.json`. The two walking guides use source scale 0.4; the lower-resolution planted guide uses its existing runtime scale 0.8 divided by the extraction scale. Guide enlargement only prepares the H3 reference, without adding claimed animation poses. Every resulting frame uses the original walking scale 0.214 divided by 0.4 and a fixed (480, 465) anatomical anchor. No per-frame repositioning, deformation, interpolation, reversal or bounding-box normalization is used.

Local H3 produced 124 frames at 24 fps, 960×544, with 20 `res_multistep` steps. Seeds were 2026092408 and 2026092409; generation took 165.636 and 159.059 seconds respectively. The actual submitted graphs preserve the chained guide conditioning and original latent connections. All 248 decoded RGB frames are verified against original server exports and preserved losslessly in FFV1, with the original MP4 viewing copies alongside them.

Review covered every corrected chronological frame, enlarged anatomy and alpha on light/dark backgrounds, and native Godot battle-size phases against the accepted idle. Four legs retain their attachment and diagonal roles, with bent joints, lifted paws, planted support and shoulder/hip weight transfer. The first attempt's exaggerated forward reach is excluded. Birch plates, amber nodules, red bindings/ribbons and rigid horns retain identity. The selected loop boundary is visually near-matched; premultiplied difference is about 0.019 compared with 0.078 median adjacent-phase difference, supporting rather than replacing visual review. No continuous manual game playtest or Linux execution is claimed.

Each attempt has its own `produce.py`. `process` reconstructs all transparent extractions from its original FFV1 video; `review` reconstructs chronological sheets; `planted_v2/produce.py build` reconstructs the selected handoff and merged candidate. Python requires Pillow, NumPy, SciPy and PyAV. Cyan keying checks corner uniformity, unmixes background color, removes cyan RGB spill and preserves separate alpha components containing opaque pixels. Submitted sources cannot be overwritten or accidentally requeued.

Six previous accepted clips preserve all 40 packed poses, timing and anatomical offsets; the overworld idle image remains byte-identical. Focused Windows validation passed 127 candidate and 141 published assertions after successful headless import. Candidate and live atlas files are byte-identical. The 31 selected RGBA frames, original art lineage, videos, prompts and workflows are retained; other extractions, duplicate server exports and temporary review/test outputs are rebuildable and removed after verification. No full repository suite is run.
