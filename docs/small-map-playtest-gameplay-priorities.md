# Small-map playtest gameplay priorities

Owner-approved highest-priority Phase 6 queue, 2026-09-12. All five slices are **completed**, including the explicit owner-approved local recruitment rule below. PLAN.md sets selection order; ops/progress.json owns operational status. Fresh Small-map terminal-match acceptance and its bounded findings are recorded below. This closes this queue, not product release readiness.

## Basis and scope

The native Windows Small (36 x 36), three-player Captain skirmish on Jade Brook Deep reached defeat on Day 16: 14 manual battles, 11 victories, two retreats and one defeat; Sunvault eliminated, Mireclaw remaining. The second Riverwatch assault lost 50 attackers against 49 defenders. The observed player experience was rated 5/10, with combat clarity and interface obstruction the main concerns. One match and imperfect assault decisions do not establish a balance defect.

Local evidence: `.artifacts/computer-use-small-rmg-20260912/playtest.md`, `02-battle-tooltip-overflow.png`, `08-riverwatch-lost.png`, `12-one-rival-defeated.png`, `16-riverwatch-defeat.png` and `17-final-skirmish-defeat.png` in that same directory. Preserve the generated `maps/small-jade-brook-deep-be633b81.amap` / `.ascenario` pair and saves. The observations below remain understandable without local artifacts.

These five slices outrank discretionary art/animation, content expansion and polish. They refine existing gameplay and input contracts; they do not reopen completed historical slices or certify release readiness. Keep project.md strategic rather than copying this queue into the charter. Related existing contracts: `docs/battle-readable-turns-and-melee-approach.md`, `docs/battle-message-log.md`, `docs/town-roster-double-click.md`, and `docs/generated-full-match-quality-requirements.md`.

## 1. Combat consequences before commitment

Slice: `ux-combat-consequence-preview-20260912`.

Observed problem: melee approach exposed duelists to retaliation and subsequent enemy attacks that rapidly destroyed them. The final approach position, likely exchange and upcoming actors were difficult to assess.

Required behavior:

- A compact preview shows the exact proposed movement destination, attack/spell target, expected damage and casualty range, and immediate retaliation where applicable before the player commits.
- Derive legality and estimates from authoritative combat rules. Previews must not consume RNG, mutate state or promise exact future outcomes where randomness or enemy choices make them uncertain.
- Distinguish the immediate exchange from subsequent enemy threats. Show the next few known actors and explain bonus/repeated actions when they occur, without exposing hidden AI decisions or debug scores.
- The committed intent matches its preview; stale/illegal intents refresh or reject safely.

Targets: BattleRules, BattleShell, BattleBoardView and their existing action/initiative presentation helpers. Acceptance: manually inspect adjacent and move-and-strike melee, ranged and spell previews; verify resulting damage/casualties against the advertised ranges, retaliation eligibility, final hex and ordered actor handoff. Repeated hover/selection must leave complete combat state and RNG unchanged.

Implemented: `BattleRules.attack_consequence_preview` enumerates authored damage rolls on isolated battle copies using the same damage modifier, legal approach, on-hit abilities, damage pressure and reflection rules as combat resolution. It bounds retaliation after casualties/status changes rather than against the untouched defender. `spell_consequence_preview` uses the existing spell resolver on its copied hero state; Advance uses the existing automatic movement resolver on a copied battle. A bounded, detached cache invalidates on full battle-state changes. `BattleBoardView` shows compact exchange text and a movement-end marker, and its initiative ribbon starts at the current living actor rather than the beginning of the round. Mouse/controller focus and action/spell buttons request these previews; playback and dismissal clear them. Existing attack summaries share the forecast. No RNG/save schema, damage balance, art or native generation changes. Damage/casualty estimates describe the primary target and immediate return, not future enemy choices or secondary area targets. The current turn-order builder schedules each living stack once per round; retaliation remains explicitly labeled separately in forecasts and ordered playback. Explicit two-stage targeting is still slice 4, not silently claimed here.

Accepted evidence: `.artifacts/combat_consequence_20260912/final-source/` passes 1,395 checks at 1920x1080; `reduced/` and `linux-package/` each pass 1,397 at 1280x720, including Advance's committed final hex. `windows-package/` passes 1,363 under headless Wine (not physical Windows GPU certification). Adjacent/approach/ranged/spell captures inspected; compact movement-end and wide spell captures show no forecast/control overlap. Includes 108 seeded exchanges across ordinary, lethal, spent-retaliation, harry, overheat, hookline and reflected-damage cases; full-state/RNG purity, cached-result isolation, live mouse/controller previews and actual ordered playback. Twenty repeat forecasts take 570 microseconds in the wide source fixture, versus 535880 microseconds before caching. Fixtures deliberately keep large health pools for playback; they are not a balance playtest. Original Windows playtest screenshots/saves referenced above are not present in this Linux checkout; the owner-recorded observations remain the requirements, not locally reproduced evidence.

Reproduction: `python3 -B tests/combat_consequence_preview_regression.py --label final-source --render --resolution 1920x1080`; add `--reduced-motion` for reduced settings. `tests/packaged_combat_consequence_preview_regression.py` runs identical assertions against isolated release binaries/PCKs with `--platform`, `--binary`, `--pack`, `--label`, and a new `--wine-prefix` for Windows. Both official export/startup checks pass (`linux-release/report.json`, `windows-release/report.json`), including Windows generated Overworld/Town entry. Both PCKs are 313226008 bytes with 5,605 identical member paths; only `project.binary` differs across platforms. No asset content/package expansion was introduced. `python3 tests/validate_repo.py` passes (log `/tmp/heroes-combat-consequence-repo-20260912.log`); existing deterministic RNG, controller-board navigation and quick-resolve reports pass under `.artifacts/full_play_runtime_20260905/combat-consequence-existing/`; `git diff --check` passes. A temporary initiative type-inference error and background export environment failures remain as diagnostic logs, not accepted results. Cleanup removed 206870664 bytes of aborted, rebuildable export output; final packages, captures, logs, user data, caches and unrelated files remain.

## 2. Bounded contextual tooltips

Slice: `ux-bounded-contextual-tooltips-20260912`.

Observed problem: hover text covered most of the battlefield or construction ledger, ran off-screen, and sometimes survived an Overworld-to-Battle transition.

Required behavior:

- Ordinary hover shows a short bounded summary positioned within the viewport, away from the active target and essential controls. Long explanations use explicit inspect/detail actions in contextual popouts.
- Hide obsolete tooltips when their target disappears, on scene/modal transitions and when pointer/focus context changes. Keyboard/controller inspection must have an equivalent dismissal path.
- Preserve scenery and the dominant play surface; do not replace overflowing tooltips with permanent text panels.

Targets: shared tooltip/inspection ownership and affected Overworld, Town and Battle controls. Acceptance: inspect long content at viewport edges in 1280x720 and a wide resolution, including 2560x1440; move focus, open/close modals and transition from map to battle. No clipped, stale or input-blocking hover surface may remain.

Implemented: `ContextualHelp` now owns short, mouse-transparent hover cards (three visible summary lines, 240-character summary budget, at most 360 logical pixels wide) and a scrollable explicit inspection dialog. F1 inspects pointer/keyboard context; controller View inspects focus, including the selected ItemList row and the Battle board cursor rather than stale mouse coordinates. Escape/controller cancel dismisses help without committing an underlying action; inspection arrows scroll. Nested inspection belongs to the originating modal viewport and does not close that parent. The existing bounded, four-line cartographic map card retains its styling/description builder and adopts the same lifecycle. Generic scriptless controls receive a presentation-only tooltip delegate; authored scripts are never replaced. Scripted scene shells, building hotspots, resources, minimap and portraits explicitly delegate. Empty text produces no popup.

Hover timing and description queries remain Godot-owned, but visible cards live in owned CanvasLayers rather than native tooltip PopupPanels. The engine explicitly supports suppressing its native popup with an invisible custom control (`scene/main/viewport.cpp`, `_gui_show_tooltip_at`, [Godot 4.6.2 source](https://github.com/godotengine/godot/blob/4.6.2-stable/scene/main/viewport.cpp)). This avoids release-template tooltip teardown disconnect errors reproduced during the first native-popup package attempt. Owner removal, changing descriptions/pointer context, input, native modal opening, Town catalogs and scene changes dispose of obsolete help. One placement pass scores viewport-contained candidates against buttons, header bands, the Battle initiative/footer and the pointed-at target; it does not scan the UI continuously.

Battle presentation revisions invalidate stale details and avoid repeating tactical path/forecast queries during stationary hover. The revision is Node metadata, not saved gameplay state.

Accepted evidence: `.artifacts/contextual_help_20260912/acceptance-guarded-small/` and `acceptance-guarded-wide/` pass 48 checks each, with actual 1280x720 and 2560x1440 captures. `linux-delivery-package/` passes the same 48 checks against the isolated final release package, without runtime errors; `windows-delivery-package/` passes 40 headless Wine checks, not physical Windows GPU certification. Coverage includes empty text, bounded real hover, full-text retention, controller scrolling/cancel, selected-row and nested-modal ownership, gameplay-state purity, stale battle revision invalidation, owner removal/hiding, construction-ledger opening and direct Overworld-to-Battle transitions. Final compact/wide edge captures and Linux inspection/ledger captures were visually inspected. Earlier failed native-popup, shell-inheritance, metadata and incorrect-resolution probes remain diagnostic evidence, not acceptance results.

Both official export/startup checks pass (`linux-delivery/report.json`, `windows-delivery/report.json`), including generated Overworld/Town entry on Windows. Both PCKs contain 5609 matching member paths and are 313236612 bytes; only `project.binary` differs across platforms. Existing combat-consequence integration passes 1397 checks in `.artifacts/combat_consequence_20260912/tooltip-integration/`. `python3 tests/validate_repo.py` passes (log `/tmp/heroes-contextual-help-owned-repo-20260912.log`); focused tracker/cartographic checks and `git diff --check` pass. Reproduce with `python3 -B tests/contextual_help_regression.py --label <new-label> --render --resolution 2560x1440`; `tests/packaged_contextual_help_regression.py` uses the same isolated Linux/Windows release bootstrap and identical assertions. Cleanup removed 2820782844 bytes in 21 superseded export files; final packages, reports/captures, user data, caches and unrelated files remain. No art, gameplay balance, save schema or native RMG changes. Three priorities and the fresh Small match remain pending.

## 3. Actual town garrisons and exposed-town warnings

Slice: `ux-accurate-town-garrison-20260912`.

Observed problem: Riverwatch showed Guard readiness 33 but the subsequent AI assault defended with zero troops. Readiness and defending strength were not sufficiently distinguished.

Required behavior:

- Show actual defending troop/stack counts, including an explicit empty garrison, separately from a plainly labeled readiness modifier.
- Use the same authoritative defender eligibility as battle setup, including applicable visiting-hero rules; do not imply a hero is defending merely because remote management is open.
- Before ending a turn, provide a compact actionable warning for an exposed, empty owned town based on available player information. Allow the player to inspect/reinforce or deliberately proceed without repetitive modal friction.

Targets: TownRules, HeroCommandRules, TownShell and Overworld end-turn/roster summaries. Acceptance: empty, occupied, transferred and visiting-hero cases show counts matching actual assault defenders. Warning updates after reinforcement, ownership changes and save/resume; it must not reveal hidden enemies.

Implemented: the scenic `Guard` plaque previously displayed `town_battle_readiness`, not troops. `TownRules.describe_defense` also called the remotely active hero a defender, while `BattleRules._town_defending_hero` compared legacy town x/y without entrance/level semantics. `HeroCommandRules.town_defense_force` now supplies both battle setup and UI with the garrison plus exactly one eligible stationed hero (preferred, active, primary, then first), using the existing stationed-hero entrance/level predicate. The scenic strip separates defending troops/stacks, garrison and readiness; roster details use the same force. Empty owned towns join the existing end-turn confirmation with an inspect-town action and deliberate End Turn, rather than another warning modal. This warning uses only known own troops and explicitly does not predict a hidden attack. No save schema, balance, native generation or recruitment destination changes.

Accepted evidence: `.artifacts/town_defender_clarity_20260912/reinforcement-source/` and `reinforcement-wide/` pass 66 source checks at 1280x720 and 2560x1440. `linux-reinforcement/` passes 70 checks against the final release; `windows-reinforcement/` passes 69 under headless Wine, not physical Windows GPU certification. Both include native Small seed 10 setup, starting-hero eligibility and full assault troop-count agreement. Coverage includes empty/garrison/visiting/remote/wrong-level/explicit-entrance/multiple-hero forces, full battle construction, save roundtrips, actual transfer/departure, ownership loss, reinforcement invalidation, inspect-town routing and live post-transfer Town refresh. Compact/wide empty/reinforced scenery and final Linux warning captures were visually inspected. The existing `overworld_end_turn_confirmation_runtime_report` also passes: cancel preserves state, stale requests reject, confirmation calls rules/autosave once, and exhausted low-risk orders retain one-click behavior. `town_frontier_status_regression.py` passes the existing ordinary/occupation/retake/combined/detached/day-refresh contract with six plaques. Both official exports/startup smokes pass under `linux-final/` and `windows-final/`, including Windows generated Overworld/Town entry. The 313240052-byte PCKs have 5609 matching paths; only `project.binary` differs between platforms. Against the previous slice, only the five intended runtime scripts differ; no art/native/data content changed. The full repository scan initially found the superseded Guard-label assertion; that contract now requires separate troop/garrison/readiness fields and retains all frontier checks. Its focused check and the complete `python3 tests/validate_repo.py` rerun pass (log `/tmp/heroes-town-defenders-final-repo-20260912.log`); `git diff --check` passes. Two later priorities and fresh Small-match acceptance remain pending.

Initial probes exposed a deferred focus call after inspect-town navigation, corrected by suppressing focus restoration when leaving the scene. Failed test fixtures used a detached encounter override, unsynchronized legacy hero mirrors and an invalid underground hero in a one-level world; those are not accepted results. The invalid-level run was stopped, and its repetitive 2472375658-byte log was losslessly compressed to 102656799 bytes. Reproduce with `python3 -B tests/town_defender_clarity_regression.py --label <new-label> --render --resolution 1280x720`; use `tests/packaged_town_defender_clarity_regression.py` for isolated Linux/Windows release probes. Generated acceptance maps are created only in isolated release user storage, never the owner's source maps.

Bounded cleanup removed six superseded export binaries/PCKs totaling 810752864 bytes; combined with lossless log compression, 3180471723 bytes were recovered. Final packages, capture/report evidence, retained test-user data and shared caches remain; unrelated files were untouched.

## 4. Consistent targeting and commitment

Slice: `ux-consistent-targeting-commit-20260912`.

Observed problem: selecting an adjacent destination could immediately start a battle, board clicks could immediately attack, and Next sometimes appeared not to change targets despite multiple legal choices. Counters sometimes appeared to include defeated stacks; the exact cause still needs reproduction.

Required behavior:

- Establish and document one understandable selection/preview/commit contract for map interactions and tactical actions. The first ordinary selection must not accidentally spend an action or enter combat; any deliberate quick-action shortcut must be explicit and equivalent across supported inputs.
- Make the intended order and committed destination/target unmistakable, with cancellation or reselection before commitment. Preserve authoritative movement, guard interception and legal combined move-and-strike behavior.
- Cycle valid live targets predictably; counts, highlights and selection agree after casualties and state changes. Explain unavailable targets without silently choosing a different victim.
- Selecting a spell enters an explicit target-selection mode with legal targets, cost and effect preview; casting occurs only on commitment. Input during playback or scene handoff cannot issue a second order.

Targets: OverworldShell, BattleShell, BattleBoardView and existing intent/selection helpers. Acceptance: mouse and keyboard/controller equivalent flows; adjacent/distant map routes, hostile/guarded interactions, melee/ranged/spells, cancel/reselect, target death and playback transitions. Only a committed order may change authoritative state, and no order may execute twice.

Implemented: ordinary map clicks now select a route, including adjacent/hostile tiles; a second activation of that unchanged selection or the existing primary order button commits it. Enter/Space and the controller route-cursor accept button are explicit commit commands; Escape/controller cancel clears a staged route. Existing arrow/left-stick directional movement remains an explicit direct-move shortcut, not a target-selection operation. Owned-town management access remains non-spending and is not reinterpreted as an attack order. Tactical board activation selects and previews; Confirm order or a repeated activation commits. Key-repeat cannot turn selection into confirmation. The existing named tactical command buttons remain explicit commands. Cancellation, state changes, playback and handoff cannot reuse a staged order; these transient UI orders are not saved.

Spell buttons now enter target mode. Authoritative spell resolution supplies legal living recipients and previews; explicit cast parameters reject a missing, dead or wrong-side recipient instead of inheriting the legacy support-spell fallback to the active stack. Legacy/default rules callers and AI retain their existing spell behavior. Thin blue legal-recipient outlines and a thick selected outline replace ordinary attack/movement cues while choosing a spell. Hover and focus describe the selected spell's cost and effect. Target-cycle controls remain available at compact sizes and share living candidates with their counts. A two-enemy fixture reproduced the unchanged Next selection: `normalize_battle_state` called `_assign_default_target` on every refresh. Normalization now retains a living player-selected opponent; normal actor handoff/default-target assignment remains separate. Roster-count fallback also no longer counts dead stacks as selectable targets. Board cancellation consumes its matching physical release after focus returns to commands, and invalid-target audio retains the existing semantic action identity.

Accepted evidence: `.artifacts/targeting_commit_20260912/guarded-source/` and `delivery-wide/` pass 95 checks each at 1280x720 and 2560x1440. `linux-controller-package/` passes 96 against the final release, including deterministic native Small seed 10; `windows-controller-package/` passes 88 under headless Wine, with the eight rendered-control bounds checks omitted, not physical Windows GPU certification. Coverage includes first adjacent/distant/hostile map selection, cancel, actual movement-rule agreement, guarded encounter entry exactly once, tactical movement/attack/spell commitment, explicit allied recipients, state/RNG purity before commitment, stale/invalid targets, living target cycles, key-repeat and controller cancel. Final spell/attack and guarded-route screenshots were visually inspected; the original corner-positioned extra-ally fixture was moved within the test battlefield to avoid its standee sitting under the initiative strip. No production art or placement changed.

Existing combat-consequence/playback integration passes 1398 checks (`.artifacts/combat_consequence_20260912/targeting-final/`). The broader physical controller/semantic-label smoke and existing full-route, movement-input ownership and deterministic RNG reports pass under `.artifacts/full_play_runtime_20260905/targeting-controller-isolated/` and `targeting-existing-rules-isolated/`. Those legacy rendered probes explicitly disable the Linux accessibility backend after AccessKit `InterfaceNotFound` process crashes; they still exercise actual Godot key/joypad events, UI labels and modal/focus ownership, but do not certify an external screen reader. Earlier failed probes and superseded static one-click/layout expectations remain diagnostic evidence, not accepted results. The maintained legacy movement/playback tests now require selection followed by confirmation.

Both official export/startup smokes pass (`linux-controller-final/report.json`, `windows-controller-final/report.json`), including Windows generated Overworld/Town entry. The final 313247892-byte PCKs contain 5609 matching paths; only `project.binary` differs across platforms. Compared with the previous town-defense release, only BattleRules, BattleShell, BattleBoardView and OverworldShell bytecode changed: no art, native code, content, save schema or balance changes. The full `python3 tests/validate_repo.py` passes (log `/tmp/heroes-targeting-delivery-repo-20260912.log`, service exit 0), as do focused compatibility checks and `git diff --check`. Reproduce with `python3 -B tests/targeting_commit_regression.py --label <new-label> --render --resolution 1280x720`; use `tests/packaged_targeting_commit_regression.py` for the same isolated Linux/Windows release assertions. Cleanup removed 15 superseded task-owned export files totaling 2010094812 bytes; final packages, screenshots/reports, retained user data, shared caches and pre-existing unrelated files remain. Recruitment and the fresh manual Small-match acceptance are not completed.

## 5. Recruitment logistics and identity clarity

Slice: `gameplay-recruitment-logistics-clarity-20260912`.

Observed problem: recruiting through remote town management immediately added troops to the distant active hero. Shard Guard / Shard Wardens, Prism Adept / Prism Adepts, Mirror Duelist / Mirror Duelists and duplicate Shard Yard labels made roles and prerequisites difficult to distinguish. Similar names alone are not proof of duplicate underlying content.

Required behavior:

- Owner decision: recruitment from a remote map site requires the hero to visit that site. Town recruitment without a stationed hero enters that town's garrison; with a stationed hero it enters the eligible local army. A distant active hero must never receive these purchases. Use existing entrance/level and stationed-hero eligibility; retain remote town-management access.
- Enforce the selected contract in live recruitment. Before purchase, show destination army/garrison, immediate versus delayed arrival, capacity and full cost; after purchase, confirm where the troops went. Preserve remote town-management access independently from troop delivery rules.
- Audit ambiguous display identities and prerequisites. Consolidate true duplicates only with content/save compatibility, and clearly distinguish legitimate different tiers, roles, variants and building upgrade paths. Preserve stable IDs unless a validated migration is part of the implementation.

Targets: TownRules, HeroCommandRules, TownShell, recruitment summaries and the affected unit/building catalogs. Acceptance: remote and local purchases, different active heroes, full armies/garrisons, lost towns, costs/reserve depletion and save/resume all respect the chosen rule. The roster, battle labels and construction prerequisites consistently identify the same content. A design note or renamed labels without verified recruitment behavior does not complete this slice.

Implemented source trace: `TownRules.recruit_active_town` delegates to `OverworldRules.recruit_in_active_town`, which previously admitted recruits directly into `session.overworld.army` without a stationed-hero check. `HeroCommandRules.town_recruitment_destination` now uses the same entrance/level eligibility and deterministic local-hero preference as town defense. Admission, spending, reserve depletion and holder mutation use that recipient. A full local formation rejects a new stack without spending; an existing stack can still merge. A distant full or empty active army is irrelevant. The existing holder writer updates active-hero mirrors only when appropriate. Site claims independently require the active hero at an authoritative interaction tile on the same level, including explicit native visit tiles rather than body anchors. Existing weekly controlled-site bonuses still increase unhired town reserves, not armies; growth/economy rules were not changed.

Muster now shows the actual destination, immediate local arrival, occupied stack capacity and total purchase cost. Cached actions refresh capacity and recipient text. Confirmation, Town log and recruitment animation follow the actual holder delta, including garrison/non-active stationed heroes. A manual generated-map visit exposed a related scenic count bug: the Town stage payload dropped the native entrance and recomputed defenders at the town anchor. It now retains entrance/level metadata and invalidates when stationed armies change; generated entrance defenders match the authoritative force.

The six ambiguous Sunvault units have distinct stats/abilities, not duplicate IDs. The legacy T1 brace/shield unit is now **Shard Pavise Guard**, the T2 volley/harry ranged unit **Prism Harrier**, and the fast T2 reach/backstab melee unit **Mirror Skirmisher**. The separate array-linked **Shard Wardens**, **Prism Adepts**, and heavier T3 **Mirror Duelists** retain their identities. Their two previously identical Shard Yard captions are **Pavise Drill Yard** and **Warden Array Yard**. Stable unit/building/prerequisite IDs, stats, costs, abilities, asset paths and saves are unchanged. Unit art/animation manifests carry the matching display names; original raster pixels and historical provenance are untouched. The frozen-cutout input guard reverses only those three exact row captions for its historical checksum, rather than rebasing art evidence. Its focused compatibility test proves an image-path substitution still fails.

Source evidence: `.artifacts/recruitment_logistics_20260912/accepted-compact/` and `acceptance-wide/` pass 175 checks each at 1280x720 and 2560x1440, including actual Muster purchase, destination cue, holder capacity/merge, wrong-level/lost-town/remote-site rejection, explicit native visit tiles, save roundtrip, costs/reserves and scenic offset-entrance defense. Their screenshots were visually inspected. Earlier setup/capacity fixture failures are diagnostics, not accepted results. Reproduce with `python3 -B tests/recruitment_logistics_regression.py --label <new-label> --render --resolution 1280x720`; `tests/packaged_recruitment_logistics_regression.py` runs the same rules/UI against isolated release packages and adds deterministic native Small seed 10 eligibility. `python3 -B tests/test_recruitment_identity_compatibility.py` passes the narrow metadata exception check without regenerating art.

Existing recruitment surface/cue, army management, field rendezvous, town transfer feedback and four elder-wild sanctuaries reports pass. The army-management legacy probe now opens the actual Log catalog instead of the removed inline tab; its transfer/input assertions remain intact. The broad town-development save/resume report is **not passing**: all 32 save/resume cases run, but Moonbite Reedshrine builds only 25 buildings within its 30-turn completion budget. The identical Moonbite result, including every per-town field and build log, reproduces against the prior `2514ed0b` Linux package (`moonbite-baseline/report.json`). This is an existing development-completion limitation, not evidence of a recruitment regression, and was not waived or retuned.

Final release evidence: `linux-accepted-probe/` passes 177 checks with inspected rendered captures; `windows-accepted-final-probe/` passes 176 under headless Wine (the rendered bounds assertion is omitted, not physical Windows GPU certification). Both official export/startup smokes pass in `linux-accepted-final/` and `windows-accepted-final/`, including Windows generated Overworld/Town entry. The final 313251044-byte PCKs have 5609 identical member paths; only `project.binary` differs between platforms (`accepted-package-parity.json`). Against the previous release, only HeroCommandRules, OverworldRules, TownRules, TownShell and the four intended content-label manifests differ. No native code, raster assets or save schema changed. The full `python3 tests/validate_repo.py` passes (service exit 0; retained `repository-validation.log`), as does `git diff --check`. Failed export attempts caused by concurrent import-lock ownership and a service without login paths are diagnostics, not game failures. One intermediate Windows package accidentally included that service's editor settings; the settings were preserved outside project scanning and the final package pair is clean.

Cleanup removed 15 superseded task-created export files totaling 2010129404 bytes (`superseded-export-cleanup.json`, `intermediate-windows-cleanup.json`). These binaries/PCKs are rebuildable. The final Linux/Windows deliverables, manual saves/generated maps, source/art, reports/screenshots/logs, retained test-user data and caches remain. Temporary Python-owned probes clean themselves; unrelated pre-existing artifact-retention files, reports and caches were not modified or staged.

### Fresh Small-match assessment

Actual mouse/keyboard play used native Small seed 10, two players, Captain difficulty, Sunvault/Solera, Winter Hearth Fen. Evidence and preserved saves: `.artifacts/recruitment_logistics_20260912/manual-small/`. The Linux release playthrough built Prism Range, recruited five Prism Harriers directly into Solera (16 to 21 troops), won a manually fought battle against 11 Roadwardens without troop losses, explored/collected resources, ended the day and entered a second guarded encounter. Twelve Tideglass Skyrays acted first and killed all 21 troops; the battle report led to genuine terminal scenario defeat on Day 2 (`45-terminal-defeat.png`). No quick resolve, save mutation, teleport or generation tuning was used. The played package preceded the scenic entrance-label correction; that correction is covered separately by the final rendered regression.

Recruitment destination/cost/result, deliberate targeting, melee move-and-strike preview and persistent battle log were readable in this bounded playthrough. The second encounter's `you larger` headcount comparison obscured the danger of stronger/faster ranged stacks; this remains a presentation follow-up, not permission to rebalance from one defeat. Initial OpenGL compatibility attempts produced tooltip teardown errors and one exit-139 crash without a usable stack trace. Resuming the save under default Vulkan completed the match. That OpenGL failure remains unresolved; passing focused probes or a two-day match is not broad hardware, balance or release-readiness certification.

## Shared validation and completion

- Mark each slice completed only after its runtime behavior is implemented and validated. Use Python-owned focused regressions and the existing repository/progress checks; avoid new report-only gates as substitutes for behavior.
- Inspect compact and wide real UI with normal, fast/instant and reduced-motion battle settings where applicable. Validate relevant source and Windows/Linux packaged flows; record physical-hardware versus Wine/headless limits accurately.
- Preserve deterministic combat RNG, saves, original art, authoritative paths/occupancy and Native RMG source semantics. Do not rebalance damage, alter generation density or add heuristic map repairs from this playtest.
- After the queue is implemented, run a fresh manual Small-map match through a legitimate terminal outcome. Assess whether players can predict actions, defend towns and understand recruitment; report tactical mistakes separately from unclear feedback or rules defects. Revisit balance only with that clearer evidence.
