# RMG native transit and connectivity correction

Status: completed 2026-09-06. Phase 6 child `bugfix-rmg-native-transit-navigation-20260905`, parent `bugfix-rmg-audit-corrections-20260905` remains in progress. Requirements: `rmg-audit-corrections-requirements.md` outcome 4; `rmg-start-placement-and-h3maped-audit.md` finding 5; `lessons-learned.md`; `h3maped-rmg-end-to-end-behavior.md`; `rmg-python-validation-workflow.md`.

## Runtime changes and source ownership

Native caves/monoliths previously adopted original-content local-offset effects instead of their source connections. Re-theming was allowed; losing the native graph was not.

- The two actual commits in `connection_cross_level_pair_0x4a6cf2` now retain reciprocal private keys. Projection validates private/source-vector/serialized identity and exports exact peer action tiles and levels. Fresh original seed-68 stops at `0x4a6fbf/0x4a7012` prove pairs 226/227 at anchor (13,5), 230/231 at (13,26), 232/233 at (35,14), and 235/236 at (24,8), with opposite levels and action tiles one column west. Evidence: `transit_owner_seed68_gate_pairs`.
- Portal projection joins actual output keys `+0x14c0/+0x14d0` to descriptor lanes `+0x2e8/+0x2f8/+0x308`. Types 43/44 retain one-way roles; type 45 retains all other matching members. Missing/inconsistent source identities fail projection. Legitimate singletons/arrival-only records retain explicit unavailable states. Fresh Medium seed-10 owner `transit_owner_seed10_monolith` at `0x4a772c/0x4a781c` proves records 959/961, anchors (59,46)/(39,30), same selected descriptor. No nearest-peer guesses, RNG consumption or H3M serialization changes.
- `NativeTransitRules.gd` validates reciprocal caves and complete descriptor groups. Old saves reconstruct only unique exact-XY/opposite-level caves or exact type/subtype portal groups from immutable source records. No save-version bump, invented rewards or lost historical ownership.
- `OverworldRules.gd` resolves real action tiles, not painted anchors. Shared player/AI safety retains impassable terrain, exact bodies/overlaps, guards, heroes, armies, towns and other interactions. Native visits never claim, repair, reward or consume the site. A paid approach includes travel; deliberate travel from the entrance costs one movement. Hero-keyed pending choices survive saves, never grant another hero or another day a free trip, and cannot erase another hero's credit. A valid unavailable portal does not make an already-paid walk fail; explicit Travel still rejects without cost.
- `OverworldShell.gd` provides a compact themed, keyboard/controller-owned destination chooser with unavailable exits disabled, confirmation through authoritative rules, and focus restoration. Hero/view layer/fog/minimap follow the exit without cross-map walking animation.
- AI routes actual objectives across layers and multiple passages through the same safety owner; saved targets and arrival agree. One field covers all destinations, with bounded caches of 32 surfaces/eight origins. Physical battle/contact/resupply remains level-local. A player global cross-portal click-path interface is not claimed.

## Starting-area connectivity: proven adoption mismatch

The real underground town entrance in Small/land/two-level/weak/one-human/one-computer seed 68 is `(8,16,1)`. Its doorway step worked, but Aurelion's both-side-blocked diagonal veto disconnected the surrounding pocket. Hypothetically opening action cells still reached only 29 cells/no cave; that diagnostic did not authorize mask removal.

Fresh original capture `transit_owner_small68_full/owner_payload.bin` and retained/fresh matched native outputs are byte-identical: **36,799 bytes**, SHA-256 `e650d82bed58f8bb4dc69bcac357d16b9bb2f99c1a29f1dea29626d783904867`. This case is not a generation terrain/footprint discrepancy.

Original `transit_small68_connectivity_owner` at `0x49eb8d` records private predecessor `(10,12,1)->(9,11,1)` in `GeneratedCell+0x10/+0x14/+0x18`, despite rock at both side cells (10,11,1)/(9,12,1). Another edge is (8,8,1)->(9,7,1). Source instructions `0x49a439..0x49a570` enumerate ordinary eight-way destinations without a both-side veto; retained source: `.artifacts/rmg_recovery/ghidra_bit21_candidate_helpers_20260710a/target_0049a318_FUN_0049a318.txt`. This instruction evidence is joined to actual private state, not treated as complete source alone.

The diagnostic CLI recomputes the workflow three times. Comparing **every invocation** in `transit_small68_connectivity_native_private/rmg_native_batch_export.log` with the owner ledger gives zero differences in all eight words `+0x10..+0x2c`, across 2,592 cells, at `0x4a8c25`, `0x4a8c2c` and pre-`0x49eb8d`. The generic comparator rejects repeated invocations; the independent ordinal-aware comparison proves each copy identical, not just a selected match. Pointer vectors and unobserved phases remain outside that comparison.

Native-package player/AI navigation now uses the recovered adjacency, selected by the existing persisted native-adoption flag. Authored maps retain their old corner rule. Actual destination terrain/body/occupancy/interaction and movement cost remain authoritative. No rock carving, mask trimming, town/hero relocation, seed exception or generator rewrite.

## Field battle handoff correction

A continuous Islands journey contacted the real turn-spawned `player_2_raid_1`. Its strategic target was distant Riverwatch. `BattleRules._normalized_battle_context_fields` inferred town defense just from that target, using the remote garrison and even losing the remote town after field contact.

Implicit defense now requires same-level physical contact at the target town's anchor/entrance. Explicit real siege/saved contexts remain authoritative. Tests cover the unchanged starting hero/army, same-town inference, wrong-level rejection, explicit context, production battle save/resume, field victory, complete remote-town equality and the actual overworld checkpoint. No combat-content or balance change.

## Validation evidence

Artifact labels below are under `.artifacts/rmg_start_audit_20260905/` unless stated otherwise. Focused reports record source hashes and reject edits during execution.

- `transit_caves_native_adjacency_journey`: PASS from the actual entrance to both other town approaches, 39/34 paid steps, one cave and two turns each, production passage saves. No journey relocation. All native object/terrain/payload controls unchanged.
- `transit_native_adjacency_caves_1280`: PASS all eight ends, strategic AI, safety, historical save, production saves/fog and rendered layers. Exact private corner passes actual player route/move, AI step, save/return, authored-corner negative control and rock-destination rejection. Both 1280x720 screenshots visually inspected: hero, layer, main/minimap fog and commands agree.
- `transit_islands_adjacency_field_contact`: PASS all six other-town approaches, up to three passages, turns/saves and a deliberate walk into a real turn-spawned raid on the first journey. Field victory preserves remote towns and returns through the production overworld checkpoint. Native controls unchanged.
- `transit_adjacency_large_all_2048`: PASS all 50 ends/65 directed trips, four connection shapes, independent hero credits, chooser bounds, safe exits, AI and saves. `transit_adjacency_portal_1280` passes representative endpoints at the smaller resolution. All four chooser/destination screenshots visually inspected: compact usable controls, correct destination, movement, fog and minimap; zero runtime errors and no source changes during either run.
- `transit_adjacency_six_ends` / `transit_adjacency_islands_ends`: PASS all nine/six-player and 14/Islands endpoints respectively, including three legitimate inactive approaches in the former. Native object/terrain/payload controls unchanged.
- `transit_adjacency_all_entrances`: PASS all 35 supported requests, 83 exact source entrance starts, live entry/exit/return and save checks. The deliberately impossible-strength request is rejected as expected. All 35 native payload FNV/byte-count pairs equal `players_all_entrances`; zero entrance failures/runtime errors. `transit_adjacency_player_regressions`: PASS all three six/eight/team cases.
- `transit_adjacency_final_repo.log`: repository validation PASS after adjacency and documentation updates. Python validation classifiers: 12 PASS, including nine transit tests; entrance-audit unit tests: 11 PASS. `git diff --check` PASS.
- `transit_final_linux_selftest.log`/`transit_final_windows_selftest.log`: PASS. Both-platform Debug/Release extensions and release selftest/CLI binaries built from the same native source, including exact source identities and missing-sidecar rejection. Native source unchanged since these builds/tests.
- `transit_adjacency_retained/summary.json`: fresh unassisted native replay still matches **23/24** retained complete owner payloads. The separate Medium normal-water seed-10 mismatch remains the only failure; the command correctly exits 1. Transit did not repair or conceal it. No authority bytes were supplied and this retained audit is not fresh executable/private-state capture.
- `transit_adjacency_packages_linux` / `transit_adjacency_packages_windows`: final export/startup PASS, both PCKs **248,438,264 bytes**, 1,561,736 below the unchanged ceiling. Windows fresh-prefix generated setup/Overworld/owned-Town flow PASS under Wine. `transit_adjacency_linux_generated`: the same three-step flow PASS in the actual Linux release binary at 1280x720; Overworld/Town screenshots visually inspected. No engine errors; host-only root/V-Sync/GLES3 MSAA warnings remain. Wine headless evidence is not physical Windows GPU certification; Linux used the established disabled-accessibility harness workaround, not a desktop accessibility certification.

Post-adjacency `.artifacts/full_play_runtime_20260905/rmg_transit_adjacency_regressions/report.json`: PASS 11/11 existing battle quick-resolve/report, movement input/full-route, fog, transactional save, AI knowledge/task execution/normalization/path reuse/assault grouping reports; zero engine errors. Earlier `transit_frozen_level_regression` passes the complete level suite; `rmg_native_transit_authored_selected` passes three rendered authored transits (their corner policy is unchanged).

Performance: isolated post-adjacency `.artifacts/large_town_build_end_turn_20260905/rmg_transit_adjacency_measured/report.json` passes three real purchases/turns on 108x108, controls-ready purchase times 2,714/2,572/2,769 ms and turns 5,473/7,691/9,717 ms. Day-four production save round-trip passes with all 2,961 source objects. This is not a method-matched old-behavior ratio or late-game certification. Profile fingerprint changed `7362cf00`->`79238f15` from additive player/transit fields. Current `transit_adjacency_profile_control` proves immutable source objects/terrain and selected map/player identity `fnv1a32:c2520619` unchanged against the retained `verified_final/day_1_before_build.json`. The old save lacks raw payload hash/bytes; derived identity is not raw-byte parity. Profile fixtures require both the new package fingerprint and unchanged native map/player identity.

## Retained failures, scope and unfinished work

- The first Small native diagnostic used wrong setup fields; it is not a control. Matched/private runs use the exact retained configuration and match all final bytes. Rejected JSON numeric-type/movement-budget assertions, mismatched comparison baselines and the visually rejected oversized chooser remain retained, not passing evidence.
- `transit_islands_native_adjacency_journey` reaches all six towns but fails the battle-evidence classifier because the improved route avoids the raid. The later deliberate-contact test preserves the classifier and walks to the real enemy; it does not inject battle state.
- Accidental default rendered bundle `rmg_native_transit_authored_paths` fails a keyboard save test expecting the older slot-bound dialog. It is not the requested authored-transit bundle or a passing full suite; its baseline classification remains unproven here.
- The first final Windows package attempt exhausted host disk space while creating its isolated Wine prefix (exit 120, empty report); it is not passing evidence. Only the two disposable Wine prefixes from this run's earlier completed `transit_packages_windows` check were removed (about 2.6 GB, reproducible by the package helper); its reports/logs/screenshots/exports and every unrelated file were preserved. The final package check was repeated successfully after that scoped recovery; the host still has little free space.
- Journeys are post-neutral-guard traversal from fresh copies of the actual generated start, not full neutral-battle balance, conquest or naval campaigns. Islands evidence proves land/portal connectivity and a real raid battle, not every boat/shipyard effect or all water-map playability.
- The separate required normal-water child retains Medium seed-10 divergence after `0x4a4c8e` and before `0x49eb8d`. Earlier Large seed-1 owner/native comparison differs already post-`0x4a8260`; exact incoming setup/first divergent rule is not isolated. Neither discrepancy was tuned away or fixed by transit sidecars. All-RMG/source-parity and parent completion remain unclaimed.

The transit child is complete within the explicit gameplay/sample limits above. The parent cannot complete before its native parity and integrated children. No art regeneration, balance change, generator RNG/topology/placement rewrite, package-limit change, unrelated cleanup or release certification.
