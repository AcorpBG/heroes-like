# Normal-water connection-vector correction

Status: completed 2026-09-06, child `bugfix-rmg-normal-water-private-state-20260905` of the Phase 6 audit-correction goal. Requirements: `rmg-audit-corrections-requirements.md` outcome 5 and `lessons-learned.md`. This source-backed correction does not complete the parent or establish whole-RMG parity.

## Source-backed fault

The baseline Medium/normal-water/one-level/weak/one-human/one-computer seed-10 mismatch was 72,595 native bytes against 72,924 original bytes. Fresh September 6 captures isolate the first divergent **grid consumer** to `0x4a79a3`, not the later mine/reward owners.

- `water_phase_boundary_owner_20260906` / `water_phase_boundary_native_20260906`: eight exact caller joins, all 5,184 cells and eight words `+0x10..+0x2c`; equal after `0x4a4c8e`, already different after `0x4a8c15` returns at `0x4ac778`.
- `water_bridge_boundary_owner_20260906` / `water_bridge_boundary_native_20260906`: equal at `0x4a8c2c`, `0x4a8cea`, `0x4a8d12`, `0x4a8d19` and `0x4a8d20`. Thus cleanup, `0x4a4913`, `0x4a5767` and `0x4a4fc5` retain identical grids. `0x4a8d22` calls `0x4a79a3`; only after it do grids differ. Source instructions: `.artifacts/rmg_recovery/ghidra_relation_owner_consumers_dump_20260607/target_004a8c15_FUN_004a8c15.txt`.
- `water_connection_calls_owner_20260906` / `water_connection_calls_native_20260906`: original RNG entering the first connection selection is `0x2c461c03`, matching native. Original source owner 0 visits four endpoint records (targets 4, 7, 10, 17); native visits six, repeating targets 10 and 17 and consuming two extra fallback draws. The original synthetic endpoint has guard dword `+0x04 = 0`, control `+0x08 = 1`, and later words 72,72,3,0. The cached native polygon record incorrectly assigns that later 3 to `guard_value` as well.

The existing `apply_ready_or_not_medium_seed10_normal_water_source_base_endpoints_4a3dbc` preloads a **later** polygon endpoint suffix before the recovered generic `0x4a3710` owner appends those endpoints itself. Its separate adjacency representation lacks that suffix, so the generic owner appends again. Original `0x4a382e/0x4a384c/0x4a3861` zero the guard and append the record once through the source object's `+0xc4` container (`+0xc8/+0xcc` data range). Source: `.artifacts/rmg_recovery/ghidra_phase_frontier_decompile_20260611/target_004a3710_FUN_004a3710.txt`.

Fresh `water_endpoint_producer_owner_20260906` captures all 22 source endpoint vectors before `0x4a3710` and at `0x4a8d20`. Owner 0 has only targets 4/7 before the producer and 4/7/10/17 afterward; owner 1 grows from 4/8 to 4/8/10/19/20. Synthetic owners start with empty vectors. Owner 10 then has targets 17/20/0/1, with controls `0x10001/0x10001/1/1` and zero guards. This proves that the cached suffix was future state, not an independent second group. The generic producer's connection view must also preserve its adjacency byte `+0x0a` processed marker (`0x4a3840`), not just the wide flag.

The duplicate future-state preload and incorrect guard interpretation are removed; the source-owned generic producer and independently required synthetic allocator fields remain. No deduplication, new seed exception, replayed final bytes, RNG skipping or changed generation density. `water_red_selftest.log` fails on the old duplicated vectors; after removal, `water_producer_selftest.log` passes. `water_producer_corrected_native_20260906` matches all five checked 5,184-cell/eight-word checkpoints, including after connection/mines/rewards and before decoration, and produces all **72,924 bytes** exactly: SHA-256 `7616922adb5865ceb727e995ff4fac72041b030bcb702c55f7a1259c413872e3`. Those initial results preceded the processed-marker correction; final-source rebuild and validation results are recorded below.

## Final-source validation

- `water_final_source_flags_native_20260906`: unassisted final production code, including the processed-marker correction. Fresh `water_same_run_private_payload_owner_20260906` captures four private grids and the entire writeout in the **same original executable invocation**. All 41,472 words per checkpoint match at `0x4a8d20`, `0x4ac778`, `0x4ac7b6` and `0x4ac826`; all 72,924 payload bytes match the SHA-256 above. Additional joins to `water_phase_boundary_owner_20260906` match post-`0x4a4c8e` and post-`0x49a1ef` / pre-decoration. All 71 ordered connection-return tuples and 39 incoming fallback RNG states match `water_connection_calls_owner_20260906` on final code.
- `water_source_flags_retained/summary.json`: **24/24 complete retained owner payloads exact**, with no owner bytes supplied to generation. Wrapper status remains the expected diagnostic refusal without authority-profile metadata; byte equality is measured separately, not a bypass of runtime gates or a claim about unobserved private state.
- Linux and Windows Debug/Release extensions rebuild successfully (`water_linux_*_extension_build.log`, `water_windows_*_build.log`). Native selftests pass on Linux (`water_source_flags_selftest.log`) and Windows under Wine (`water_final_windows_selftest.log`). The new vector test was first run red against old production code (`water_red_selftest.log`).
- `water_final_rendered_1280` and `water_final_rendered_2048`: the exact corrected seed-10 map passes entrance actions and all **37 live checks** at 1280×720 and 2048×1079, including real native guard battle handoff/completion/resume, AI doorway egress, complete deterministic turns, main-map/minimap/fog and production disk save/restore. Both starting heroes equal their owning entrances `(29,21,0)` and `(43,20,0)`. Live payload identity is `21abc311` / 72,924 bytes, independently matching FNV-1a over the byte-exact native payload. Both screenshots were visually inspected; the hero is at the scenic doorway, controls remain visible, and fog is present.
- `water_final_packages_linux/report.json` and `water_final_packages_windows/report.json`: Linux and Windows export/startup checks pass, both PCKs **248,438,264 bytes**, **1,561,736 bytes below** the unchanged ceiling. Windows also passes fresh-prefix generated setup → Overworld → owned Town under Wine. `water_final_linux_generated/live_validation_report.json` passes those same three steps in the actual Linux release binary at 1280×720, with zero errors; its Overworld and Town screenshots were visually inspected. This is not physical Windows GPU certification.
- `water_final_all_entrances/summary.json`: **37 requests, 36 supported generations, 85 exact town-entrance starts**, all 36 primary-hero movement/entry/return/save cases pass, zero runtime errors. The single invalid-strength request is correctly rejected. This adds the exact failing normal-water seed 10 to the previous matrix. All previous 35 supported cases retain identical native payload hashes/lengths, objects, terrain, starts and player slots against `transit_adjacency_all_entrances`; only the corrected additional case intentionally changes from its failing native baseline.
- `water_final_repo.log`, 11 entrance audit unit tests, 12 player/transit validation classifier tests and `git diff --check` pass. The parent remains active for the integrated-regression child, including the stale XLarge subtype-45 dwelling expectation.

Reproduction uses `python3 tools/rmg_retained_authority_audit.py --label <fresh_label>`, both rebuilt `bin/h3maped_rmg_core_selftest` executables, and `python3 tools/rmg_native_batch_export.py --controlled-case water_final:medium:2:10:normal_water:1:1:1:1:2:-1:1:1:1:0:-1 --include-unsupported --emit-final-h3m-payload --out <fresh_path>`. For checkpoint/call joins, enable `AURELION_RMG_TRACE_POST_4A4C8E_GRID`, `POST_4A4FC5_GRID`, `POST_4A8C15_GRID`, `POST_4A9D6A_GRID`, `POST_4AAB7E_GRID`, `POST_49A1EF_GRID`, `PHASE_RNG` and `4A696B` (each with the full `AURELION_RMG_TRACE_` prefix). Preserve the original caller-PC/register joins described above, rather than selecting matching snapshots from repeated invocations. Exact capture inputs and debugger commands are retained in each ledger. The combined private/writeout ledger SHA-256 is `31e19be0db6c68479eeeb64f13abdbb6317adcfce2e904698d060a95e1a51dce`; final native trace SHA-256 is `a3f5a0aa911f98975bacf6824add9175a5d6b8c23c923c0b5a695491b40b6e96`.

Disk scope: after inspecting the passing transit package report and confirming no process used its prefixes, only this goal's two disposable `transit_adjacency_packages_windows/{wine-prefix,generated-wine-prefix}` directories were removed to provide space for fresh Windows validation. Their reports, screenshots and exports remain. Prefixes are rebuildable; no unrelated retention files, reports, source captures or user saves were removed.

All new artifact labels are under `.artifacts/rmg_start_audit_20260905/`. Captures use the clean owner executable and artifact-only established seed patch from the original audit; no foreign binaries/assets are packaged. The grid joins merge overlapping dumps, require every cell and reject conflicting words or duplicate native invocation indices. They do not certify pointer vectors or unobserved phases; the connection-call and producer captures separately establish the named endpoint fault. This correction is not arbitrary-seed, allocator-history, whole-H3MapEd or release certification.
